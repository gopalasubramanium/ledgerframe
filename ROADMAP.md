# ROADMAP.md — LedgerFrame Parked Items

**The rule: nothing on this list is built without a plan file in `docs/plans/`.**
These are accumulated breadcrumbs from the v2 decision audit — deliberately *not*
v2 work. Each entry is parked until a plan file covering it exists. Some carry
explicit gates or conditions (below) that a plan file must additionally satisfy.
Source: `docs/audit/DECISIONS.md` (D-001–D-080). Entries mirror that file's
merges — the historical-FX item is a single merged entry (D-020 + D-076).

---

| # | Item | Origin | Conditions / gates | Status |
|---|------|--------|--------------------|--------|
| R-1 | Optional passphrase mode (8–64 chars) | D-002 | — (PIN remains an access lock, not data-at-rest protection) | parked |
| R-2 | User-requestable transaction currencies | D-006 | Must be FX-validated — a currency may exist in the master only if the FX service can translate it | parked |
| R-3 | Domicile for fund-tax display | D-007 | Reintroduces a domicile field beyond the single authoritative `listing_country` | parked |
| R-4 | Instrument notes | D-015 | Follows the drop of the general `Note` table; per-record note fields cover the current need | parked |
| R-5 | Opt-in AI chat history | D-016 | Opt-in only; default remains ephemeral (Product Guarantee 6 — no stored AI conversations) | parked |
| R-6 | `spec` (specific-lot) cost-basis method | D-018 | Extends the per-account fifo/average selector | parked |
| R-7 | Corporate actions — de-merger / spin-off + ticker/symbol rename (v2.1 "accounting precision") | D-019 | **Owner-identified 2026-07-10.** De-merger/spin-off support + exposing a symbol/ticker rename. Splits/bonuses/mergers already engine-covered; see the v2.1 theme for the plan-file coverage. | parked |
| R-8 | Historical FX series (enables trade-date backfill + per-date realised totals) | D-020, D-076 | Merged entry. Native-currency stays the filing-grade output; base-currency realised totals stay explicitly indicative with caveats preserved | parked |
| R-9 | Insurance cash value: opt-in inclusion in Net worth | D-039 | Opt-in only; default remains permanently excluded in v2, stated visibly on Insurance and Net worth pages | parked |
| R-10 | App-wide Detail level | D-040 | Gated on per-page specs (v2 scopes Detail level to Home only) | parked |
| R-11 | User-defined scenario shocks | D-058 | Gated on a proper plan file; "scenario, never a forecast" preserved. **⊕ SEAM NAMED (2026-07-15, page-scenarios §9-7):** the fixed-shock magnitudes are now **named constants with rationale** (`EQUITY_SHOCKS`, `RISK_SHOCK`, `CRYPTO_SHOCK`, `PROPERTY_SHOCK`, `FX_SHOCK` in `services/scenarios.py`) — so this becomes a **config change over an existing seam, not a rewrite**. **⊕ UPGRADED (2026-07-15, page-scenarios §12sc1-2):** the target shape is **interactive sensitivity analysis** — a **per asset-class / currency / region shock SLIDER** over a **PARAMETERIZED scenario endpoint** (the server computes the impact; **no client money math** — D-105); **debounced** requests; the **D-058 copy bar applies to the exploratory surface** (a scenario, never a forecast, even while dragging). **Enabled by** the §9-7 named-constant seam and the §9-4 single-derivation exposures. **Requires its own plan file.** | parked |
| R-12 | Revisit AI validator strictness | D-070 | Only if fallback frequency proves high in practice; the validation contract may not weaken (Product Guarantee 7) | parked |
| R-13 | Per-lane provider priority editing | D-072 | Only on demonstrated need; no user-editable provider priority in v2 (visibility yes, editability no). **⊕ AFFIRMED STILL PARKED (2026-07-18, data-feed-routing §9-R):** R-38 (provider routing matrix) is a **different shape** — a cell selects *one provider per class×country*; it does **NOT** reorder a lane's priority chain and does **NOT** unpark R-13. Lane-priority editing remains parked; Pricing Health stays read-only (D-072). | parked |
| R-14 | FD accrued-interest valuation | D-073 | First post-v2 feature. Plan file **must cover day-count conventions, compounding variants, maturity handling, and provenance labelling of calculated values**. `calculated_accrual` retained in the ValuationMethod vocabulary but no v2 lane emits it | parked |
| R-15 | User-configurable review thresholds | D-084 | The v2 defaults are owner-set (D-084: runway 3 months, goal 180 days, the rest as audited); this makes them user-editable. Each still a named constant with a rationale | parked |
| R-16 | Ticker scroll speed as a per-device display setting | D-047-amend / D-078 | **Owner-identified 2026-07-11 (page-chrome §11-18).** The ticker scroll duration (+ height/density) are tokens today; this exposes scroll speed as a **per-device** display axis (D-078, localStorage — like theme/density), for the **future Settings page plan**. NOT built now; default speed owner-ratified at 30s | parked |
| R-17 | Ticker index entries link to Markets | D-047-amend / D-098 | **✅ SHIPPED 2026-07-13 with the Markets build (page-markets ND-5, §12mk3-3; page-chrome §11-19 CLOSED).** `fetchTickerQuotes` sets the index `href` → `/markets` (holdings still → `/instrument/{symbol}`, D-098); owner verified the index-click lands on Markets live. Unit test (`chrome.test.ts`) pins the mapping | **DONE** |
| R-18 | D-101 themed scrollbar-thumb polish | D-101 | Parked cosmetic from Instrument Detail (the themed thumb didn't paint in **headless captures**; the structural fix — header-owns-gutter — is verified). **NOT addressed in page-chrome batches 1–4** (no scrollbar CSS touched). Named home for the polish/verification; a real browser is now available (ADR-0004 chromium) to confirm the thumb paints and close it | parked |
| R-19 | Customisable Home — movable/resizable widgets, user-selected set | D-046 (amends) / owner 2026-07-11 | **Amends D-046** (Home was deliberately simplified to a fixed set) — the plan file is a **decision amendment, not an addition**. Gates: the widget catalogue = **canonical-page summary readers ONLY** (P-1 + the enforcement corollary apply **per widget** — no widget adds a figure its canonical page lacks); the **D-046 fixed set stays the default + reset target**; **rotation / wall-appliance behaviour defined for custom layouts**. **v2 Home still builds to D-046 unchanged.** | parked |
| R-20 | External widget API — scoped long-lived keys for read-only summary widgets (Home Assistant / MagicMirror / kiosk) | D-069 / owner 2026-07-11 | Builds on **D-069 token semantics** (create / name / revoke; shown once; revoke severs). Gates: **read-only ONLY — no write scope exists**; **per-key scoping**; operates within the **D-001 posture** (LAN/VPN reach, never internet exposure; the plan file **must address the key-over-LAN threat model given no TLS in v2**); **inbound-serving vs no-egress semantics stated explicitly**. | parked |
| R-21 | External-connectivity adaptors (FIX, ISO 20022) as new **provider lanes** in the existing provider/routing model | Product Guarantee 1 & 5 / owner 2026-07-11 | Gates: **FIX = market-data / drop-copy READ-ONLY** — no order flow, no order endpoints (**Product Guarantee 1, protected**); **ISO 20022 = statement / position ingestion only**; adaptors **inherit provenance labelling**; under **no-egress every adaptor makes zero calls** and freshness degrades to **honest stale-flagging (Guarantee 5)**. **One plan file per protocol.** | parked |
| R-21a | MCP surface — recorded separately | D-067 / D-068 / owner 2026-07-11 | **AI-tool integration**, kin to the **AI-surfaces milestone** (D-067/D-068 pipeline), **not a market-data lane** — evaluate there, not as an R-21 provider adaptor. | parked |
| R-22 | AI provider configuration — local (Ollama) vs cloud API | D-067 / D-068 / D-069 / owner 2026-07-11 | **Explicit input to the AI-surfaces milestone plan** (D-067/D-068), not a standalone build. Gates: **API-key handling via the D-069 token pattern** (create / name / revoke; shown once; never returned); **no-egress interaction is normative — under no-egress AI is local-only (Ollama), a cloud provider makes zero calls** (D-075/D-060, honest degradation); the plan **decides whether first-run/Settings gain an AI configuration step there** (not this first-run milestone — D-045 stays the five steps, P-7). | parked |
| R-23 | RSS / user-defined news feeds as a **news provider lane** | owner 2026-07-11 | A user-defined feed lane in the existing provider/routing model. Gates: **provenance labelling** — each item carries its feed source, never presented as first-party; **zero calls under no-egress** (the lane degrades to honest stale/empty, Guarantee 5), like every other provider lane. | parked |
| R-24 | First-boot **license acceptance gate** | owner 2026-07-11 | **NOTE: cannot be a D-045 checklist step** — skippable ≠ acceptance. Would be a **separate blocking gate** (mounts before/around the shell, not the skippable first-run overlay). **Revisit only if the licensing / distribution posture demands it.** **RESOLVED 2026-07-14 (release-readiness RD-3): NOT needed for the v2.0 SOURCE release — cloning implies reading the LICENSE. R-24 STAYS PARKED.** Its revisit is now bound to a milestone, not a mood: **it is reconsidered if/when PACKAGED binaries ship** (release-readiness RD-1c), because that is the case where the user never sees a repo. *(The owner's earlier 'strike this and build it now' alternative is hereby closed.)* | parked (revisit at **packaged**) |
| R-25 | Personal-profile fields (name, for personalization) | D-045 (amend) / PRODUCT-SPEC / owner 2026-07-11 | **HARD GATES:** requires a **deliberate D-045 / PRODUCT-SPEC no-profiling amendment** (the current spec explicitly forbids profiling); **the owner must re-affirm it against the privacy identity** before any build; **DOB / gender remain out** regardless. Not a gap — a deliberately-declined-until-amended item (see page-first-run-checklist §12). | parked |
| R-28 | **Liquid-vs-illiquid net-worth trend decomposition** — a stacked-area Net worth trend split by liquidity rung | owner 2026-07-12 / page-net-worth §12b1-4 | **FORWARD-ONLY + OPT-IN** (mirrors the ND-1 no-backfill honesty ruling). Grounds — verified §12b1-4: `net_worth_snapshots` stores **scalar totals only** and `portfolio_snapshots.detail_json` is **written empty**, so **no historical split exists**. Gates: the worker **persists a liquidity/class split going forward** (snapshot detail — column vs `detail_json`, **shape TBD in the plan file**); **NO BACKFILL** — historical reconstruction needs **historical FX (R-8, parked)** and would **fabricate a split for manual assets** (property/cash have no price series, contra Guarantee 3); a **`PriceChart` stacked-area mode is a DESIGN-SYSTEM §5 amendment gate**; **plan-file gate standard** (nothing built without it). | parked |
| R-29 | **Implicit "seen" state for Review attention items** — a distinct concept from **Mark reviewed** | owner 2026-07-13 / page-review §12rv1-2 | Owner raised auto-recording a review on full-scroll + navigate-away — **DECLINED for v2** (§12rv1-2): (a) `POST /review/log` is **`require_auth` [S]** — a gated write must not fire silently; (b) a recorded review is a **deliberate `ReviewLog` attestation** (GLOSSARY), and scroll-past is not review — auto-attesting violates the trust posture; (c) it would **pollute Review history**. A future "seen" marker is a **separate concept** from Mark-reviewed and **requires its own concept, auth posture, contract delta + state model, and a plan file** (nothing built without it). Distinct from **per-signal acknowledge/dismiss** (ND-5, also ROADMAP). | parked |
| R-30 | **Postgres backend option** | owner 2026-07-14 | **Hosted-tier concern, paired with D-001's proprietary layer.** **SQLite is the appliance-correct choice for v2** — the file-based backup / restore / install story was **ratified at Gate B** (`backup.sh` + the `update.sh` backup gate + a single data dir *are* the recovery model, and they are the model precisely because the database is one file). A Postgres option is a **hosted-tier** need, not an appliance need; a plan file must state what it does to the backup/restore story, since that story is what a stranger installing the appliance actually depends on. | parked |
| R-31 | **Internationalization** — backend string catalog + per-locale GLOSSARY with parity guards | owner 2026-07-14 | **Platform milestone, and it is ENABLED BY THE ARCHITECTURE WE ALREADY BUILT:** because every user-facing label, vocabulary and money figure is a **served display string** (D-005 frontend-zero-copy; D-105 backend-formatted money), the strings are already in ONE place — the backend — instead of scattered through the components. Gates: a **backend string catalog** (the served labels become catalog keys); a **per-locale GLOSSARY**, since the hard rule is that every displayed term exists in GLOSSARY with that exact spelling — which must hold **per locale**, not just in English; and **parity guards per locale** (`test_glossary_parity.py` generalised — a locale that ships a term the spec lacks fails, exactly as today). Money/number/date formatting stays **backend-side** (D-105), so locale formatting has one home too. **⊕ LOCALE DATE FORMATTING (added 2026-07-15, page-cash-flow §12cf1 walk):** the owner ruled that **served ISO dates STAND** for now (`2026-09-30`) — **platform-consistent** is worth more than per-page prettiness, and a date format is a **locale** decision, not a page one. Formatting dates therefore lands **here**, once, for every page. | parked |
| R-32 | **Voice — DEFINITION PENDING (owner)** | owner 2026-07-14 (RD-9 Timing Amendment 3) | **Named as a RELEASE-SCOPE GATE on 2026-07-14 — and it is the only item in that gate with NO definition anywhere:** no spec, no decision, no plan file, no prior ROADMAP entry. **It requires an OWNER DEFINITION and its own milestone plan before the release gate can even be EVALUATED against it.** ⚠ **No Voice behaviour has been invented, assumed, or inferred.** *What exists today is NOT a definition:* an optional `voice` extra (**vosk** + **sounddevice**) in `pyproject.toml` and an install-time prompt in `install.sh`. That is a **dependency and an installer option** — it says nothing about what Voice IS, what it does, what it may read, or what it may never do. Gates a definition must set, at minimum: **scope** (dictation? command? read-out?); **the Guarantee-1 line** (the platform never executes trades — a voice surface may not become an instruction channel); **the Guarantee-5 line** (no-egress — is recognition local-only, and does audio ever leave the device?); and whether it rides the **P-6 single AI pipeline** (D-067/D-068) or is a separate lane entirely. **This item blocks the release gate until the owner defines it.** **⟶ ANNOTATION (owner, 2026-07-18, page-settings §14st-2):** the Settings **"AI" tab** was named **"AI", NOT "AI & Voice"** deliberately — naming an undefined surface would invent behaviour. The tab **gains "& Voice" only after this R-32 definition lands** (a rename + whatever Voice controls the definition authorises); until then the AI tab carries the AI-config line only. **⟶ RELEASE SCOPE (owner ruling, 2026-07-18, RD-9 Amendment 4):** Voice is **POST-RELEASE** — it **does NOT gate v2.0.0** (Amendment 3 had named it a release gate; Amendment 4 moves it out). Its **definition remains owed**, and it **gates whichever release ships it**. See `release-readiness.md` RD-9 Amendment 4. | **DEFINITION PENDING (post-release)** |
| R-33 | **Per-entity investment policies** | owner 2026-07-14 (page-policy §9-21) | Today there is **one household policy**: a single active `InvestmentPolicy` row with **no entity FK** on it or `PolicyTarget`. That is why `GET /policy/drift` now **rejects `?entity_id` with a 400** rather than serving a precise-looking, meaningless comparison (an entity's actual allocation measured against a policy that was never its own). Making policy per-entity is a **data-model change** (entity FK + a policy-per-entity resolution rule + a household-vs-entity default), not a query param. Gates: what the **household** view means when entities have their own policies (roll-up? a separate household policy?); which policy **Review** summarises; and the **Reports Pack's** per-entity sections (P-3). | parked |
| R-34 | **Goal surfacing beyond Cash flow** | owner 2026-07-15 (page-cash-flow §12cf1-5) | **Recorded answer:** goals surface **canonically on Cash flow** + **Review's goal-soon signal** — and **nowhere else, for now**. Two candidate homes are named so they are not smuggled in as riders: a **Home widget** (an **R-19** evolution — the customisable-Home work, where the widget catalogue is *canonical-page summary readers only*, P-1) and a **Reports Pack section** (**decided at the Reports plan**, not here). **Deliberate scope, not an omission.** No UI change now. | parked |
| R-35 | **Per-entity planning** (goals / obligations / contributions) | owner 2026-07-15 (page-cash-flow §9-15) | None of the three models has an entity FK and no endpoint accepts `entity_id`, so Cash flow is **household-only by construction**. Making planning per-entity is a **data-model change**, not a query param. **Also covers per-entity SCENARIOS** (page-scenarios §9-8 — `/portfolio/scenarios` rejects `?entity_id` today; per-entity what-ifs land with this). Gates: what the **household** view means once entities have their own records; which set **Review** summarises; and the **Reports Pack's** per-entity sections (P-3). **Scope sketch (page-accounts §14ac-4, owner realization 2026-07-16):** also covers **entity- and institution-level FILTERED VIEWS** over Accounts/Holdings — a filter over the account *attribute* (the account-scoped chip precedent, Amendment G), distinct from the per-entity planning *models* above. Per the §9-8 ruling (entity is an account attribute, not a page filter; `?entity_id` stays dormant with **no** UI caller until this activates), these are **NOT batched** onto page-accounts — a pull-forward is **R-35 activation with its own plan file**. | parked |
| R-36 | **Insurance premiums → Cash flow (derived integration)** | owner-requested 2026-07-16 (page-insurance §14in-3) | **Owner calls the current separation *"disintegrated"*** and wants insurance premiums to flow into Cash flow automatically. **NOT built now — no behaviour invented.** A plan file must decide: **derived read-only line vs suggested entries**; **double-count handling** (premiums a user already entered as obligations); **Cash flow's §0-PROTECTED D-057 invariants untouched** — a derived line must **not** enter the user register or the `once`-exclusion semantics silently (contributions never reduce runway; `once` excluded from recurring burn); **one derivation** — the SAME per-policy annual-equivalent served by §14in-2 (`_annual_premium`), never re-derived; **deletion/lapse semantics** (a lapsed policy's premium must leave the projection); and **which figure Review summarises**. | parked |
| R-37 | **Rotation engine** | D-044 / D-017 / owner 2026-07-18 (page-settings §9-2(b)) | **Parked from page-settings §9-2(b)** — the three write-only allow-list keys (`rotation_seconds`, `rotation_pages`, `focus_page`) were **REMOVED** this milestone (D-078 forbids a key with no consumer; shipping keys without the engine was the defect, and shipping the engine inside Settings was scope creep). The engine and the three keys are **re-added TOGETHER, spec-first**. Scope: a **chrome-level rotation engine** honouring **D-044** (configurable page set + interval; **skips errored/empty pages**, D-044), server-persisted config (D-017 — the config lives in settings rows, not localStorage). **Named prerequisite:** the **per-page health signal** the skip rule needs (a page must be able to report errored/empty so the engine can skip it) — no engine without it. The existing TopBar rotation toggle (`AppShell.tsx`, local-state-only today) is the chrome control this engine wires — **HIDDEN 2026-07-18** (owner-ruled at the Settings Phase-0a gate, dead-affordance principle; page-chrome delta note) and **restored here** when the engine lands. **Owner-ruled at the same gate (page-settings §12 ruling (e)):** when the engine lands, **sweep the served defaults surface (`rotation_seconds` in `GET /settings.defaults`, `settings.py:69`)** — a served read-only default left in place this milestone rather than repeat the §9-2 scope-creep concern. **Named prerequisite:** the per-page health signal the skip rule needs. **Plan-file gate standard** (nothing built without it). | parked |
| R-38 | **Provider routing matrix** — per asset-class × listing-country provider mapping | owner 2026-07-18 (page-settings milestone close) | **⚡ ACTIVATED as the NEXT milestone (owner ruling 2026-07-18) — own plan file `docs/plans/data-feed-routing.md`, the R-35-activation precedent (plan-file-first, verify-first).** The **KEPT machinery** (verify-first, confirmed): `CAPABILITIES` already declares `asset_classes`/`regions` per provider (`05-PROVIDERS-AND-ROUTING.md` §A.2; `app/providers/market/router.py:53`, dataclass `:44-45`); the pure resolver already takes `asset_class`/`asset_subclass`/`listing_country` and validates coverage (§A.7; `router.py:195` `route()`), and a per-instrument `source_override` precedence slot already exists (`router.py:237`). **MISSING (the plan's scope):** the **mapping table** (a new data model — per asset-class × listing-country → provider); its **precedence slot** (between the per-instrument override and the active provider); **capability-validated editing** (a cell may only pick a provider whose `CAPABILITIES` cover that class×country); the **matrix editor UI** — canonical home **Settings → Data feeds** (§14st-1); and how **Pricing Health** surfaces **per-cell provenance** (which provider actually priced each class×country). **NOT built in the Settings milestone** — plan-only kickoff follows the close. | **ACTIVATED (next; plan-only)** |
| R-40 | **Alpha Vantage premium feed expansion** | owner-raised 2026-07-18 (data-feed-routing §9 one-pass) | **DEFINITION OWED by the owner.** Premium-tier AV feeds **beyond the current lanes** (e.g. fundamentals / additional series) MAY serve future platform needs. A plan file must first have the owner name **which** feeds, serving **which** page/surface, before any build. **Input material, NOT a plan source:** the owner-held vendor documentation — the plan cites adapter code (`external.py`), never vendor PDFs (the plan-source discipline). **NOT R-38 scope:** R-38 routes *existing* lanes only; it does not add feeds. **Plan-file gate standard** (nothing built without it). | parked (definition owed) |
| R-39 | **chrome-sidebar-refresh** — the sidebar visual/interaction refresh | owner 2026-07-18 (page-settings §14st-3, re-walk) | **Owner-decision, DEFERRED to its own milestone** (§14st-3, the second re-walk ruling). Sequenced as the **FINAL pre-release milestone** (after data-feed-routing · Help · Legal · AI-surfaces). **Translation constraints recorded now so the milestone inherits them (nothing invented beyond these):** (a) **semantic-only colour** — the refresh honours DESIGN-SYSTEM's semantic-only-colour rule, no decorative palette; (b) **no avatar / account block** — this is a single-user appliance (D-001); the sidebar gains **no** profile/avatar/account affordance; (c) **collapse-to-icon rail rides the EXISTING D-078 sidebar-collapsed setting** — the icon rail is the collapsed state of the already-built per-device sidebar-collapsed toggle, **not** a new persistence axis. **Plan-file gate standard.** | parked (final pre-release) |
| R-41 | **Per-provider credentials** — a write-only key **per market-data provider** | owner-surfaced 2026-07-18 (data-feed-routing §16, R-38 Phase 3b re-walk) | **Surfaced BY R-38.** The routing matrix invites **multiple keyed providers concurrently** (a cell may select any capable provider), but the single shared `LEDGERFRAME_MARKET_API_KEY` slot (`app/api/v1/routes/system.py:140`) **cannot** support that — today the provider table can only honestly show SET on the **one active** keyed provider (data-feed-routing §14 key-slot ruling). **Scope sketch (owner to ratify at plan-time):** per-provider **write-only** key storage; the Settings key control gains a **provider dimension**; the matrix **caveat chip** keys off **per-provider** credentials instead of the shared slot. **Plan-file gate standard.** **PARKED — owner prioritises** (release-scope candidate). Cross-ref: data-feed-routing.md §16. | parked (owner prioritises) |
| R-42 | **Intraday price series** — sub-daily bars (e.g. 1-min for 1D) | owner-raised 2026-07-18 (data-feed-routing §18, §14dr-7); **ACTIVATED 2026-07-18 (owner definition, data-feed-routing §20 / §14dr-13 re-walk)** | **⚡ ACTIVATED as the milestone immediately after data-feed-routing closes, BEFORE Help (owner ruling 2026-07-18) — own plan file `docs/plans/intraday-series.md` (stub now, R-38 stub pattern).** Surfaced BY §14dr-7 (the disabled 1D/5D). Today every live provider fetches **daily only** (`yahoo.py:209` `"1d"`, `external.py:213` `"daily"`). **Owner DEFINITION (2026-07-18):** intraday series is **(a) tier-aware** — respects the learned `av_tier`; the free tier keeps the honest **disabled** 1D/5D, a premium tier enables it; **(b) USER-TRIGGERED** — an explicit fetch **per instrument / per range**, never a background poll (budget discipline: `alphavantage free ≈ 25 req/day`); **(c) PERSISTED permanently once fetched** — stored + reused, no re-fetch on view. **KEPT machinery:** the `PriceHistory.interval` column (`app/models/__init__.py:325`) is the storage seam. **MISSING (plan scope):** the intraday fetch adapter path (intraday interval, not `"daily"`); range→interval mapping; per-instrument/range trigger + persistence; chart consumption (the dr-7 disabled ranges re-enable here) — all deferred to the plan file. **Plan-file gate standard.** Cross-ref: data-feed-routing.md §18 dr-7, §20 dr-13. | **ACTIVATED (next-after-R-38; plan-only stub)** |
| R-43 | **Historical valuation backfill** — retrospective portfolio valuation | owner-raised 2026-07-18 (data-feed-routing §18, R-38 Phase 3b re-walk batch 3) | **Owner priority — today's Net worth trend is flat/linear** because only forward snapshots exist (cf. R-28 grounds: `net_worth_snapshots` stores scalar totals only). **Scope:** retrospective portfolio valuation from **price history + transactions + per-date FX** — so it **needs R-8 (historical FX series)**; **Decimal engine** (no client money math, D-105); **snapshot persistence**; powers **Net worth trend + performance**. The **biggest** of the three batch-3 requests. **Plan-file gate standard.** **Release-pull candidate** (owner's call). Cross-ref: data-feed-routing.md §18; R-8; R-28. | parked (release-pull candidate) |
| R-44 | **News thumbnails** — instrument / News / Home article images | owner-raised 2026-07-18 (data-feed-routing §18, R-38 Phase 3b re-walk batch 3) | **New egress** — `og:image` / RSS `enclosure` image fetch, so the plan **must** make it a **no-egress-mode conditional** (zero calls under no-egress, honest fallback — Guarantee 5, the provider-lane precedent); **caching** of fetched images; **layout** on the instrument / News / Home surfaces. **Plan-file gate standard.** Cross-ref: data-feed-routing.md §18; D-075/D-060 (no-egress symmetry). | parked |
| R-45 | **Per-instrument + default news coverage** | owner-raised 2026-07-18 (data-feed-routing §14dr-29 re-walk) | **AMENDED SCOPE per the dr-29 verification:** three `DEFAULT_FEEDS` **already exist** (`app/services/feeds.py:37-41`) and Home headlines are **holdings-scoped** (`Home.tsx:67`) — an erased instance legitimately shows none (NOT a bug). R-45 is therefore **(i) per-instrument news source** (owner proposal: `finance.yahoo.com/quote/{TICKER}`; whether a stable feed/API exists vs scraping is an **OPEN QUESTION — do not assume**) and **(ii) default-coverage expansion** so typical holdings actually yield headlines. **Egress ruling required** (P-8 symmetry, no-egress conditional — same decision class as R-44; **take the two rulings together**). News is a **closed page** → dated delta notes + pre-pass re-runs on touch. **Plan-file gate standard.** **⚡ PULLED INTO v2.0.0 by owner ruling 2026-07-18 (RD-9 Amendment 5).** Cross-ref: data-feed-routing.md §14dr-29; R-44. | parked (v2.0.0 scope) |
| R-46 | **Home summary cards (post-close pages)** | owner-raised 2026-07-18 (data-feed-routing close) | Elevate Home with summaries from pages built **after** it: **(i)** Cash-flow runway, **(ii)** liquidity what-ifs (Scenarios), **(iii)** upcoming insurance renewals, **(iv)** goal progress (Cash flow). **Rationale:** pages built after Home should elevate Home. **Constraints:** Home is **closed/accepted** (dated delta notes + pre-pass re-runs); **summarise-with-reader ONLY** per the IA ownership tables — **no re-derivation** (P-1 + the enforcement corollary per card, the R-19 precedent); **D-046/D-047 Simple/Full** composition; **ratified components only**. **Plan-file gate standard.** **⚡ PULLED INTO v2.0.0 by owner ruling 2026-07-18 (RD-9 Amendment 5).** Sequencing suggestion (architect, owner to rule): **adjacent to R-39**. Cross-ref: R-19; R-34. | parked (v2.0.0 scope) |
| R-47 | **Intraday compaction / downsampling** — an honest release valve for intraday-row growth | R-42 §9-1 ruling (owner 2026-07-18) | **Filed BY R-42.** R-42 rules intraday retention is **truly forever — no prune, ever** (a guarantee that silently prunes is not a guarantee). The honest growth math (≈390 candles/instrument/trading-day for 1D 1-min; user-triggered per range, so only viewed windows persist) is bounded in practice, but a heavy long-lived instance could still accumulate. **Scope (owner to ratify at plan-time):** downsample or archive **older** intraday windows to a coarser granularity (never delete a window the served series still claims to hold at its stated interval), or a documented keep-last-K-fetched-days policy — **manage bloat by roadmap, never by silent pruning**; the retention guarantee and the served series' honesty about its granularity stay unbroken. **Plan-file gate standard.** Cross-ref: intraday-series.md §9-1. | parked |
| R-48 | **Benchmark on intraday ranges** — benchmark overlay for 1D/5D on Instrument Detail | R-42 §9-5 ruling (owner 2026-07-18) | **Filed BY R-42.** R-42 is **Instrument-Detail-only** and **hides** the benchmark overlay on 1D/5D with a served reason (**"Benchmark comparison is daily-range only"**) — the benchmark rides a daily series (`analytics.py:238-242`) and no intraday benchmark join exists. **Scope:** an intraday benchmark series + a **tz-aligned** per-bar join so 1D/5D can draw the two-line comparison honestly. **Plan-file gate standard.** Cross-ref: intraday-series.md §9-5. | parked |
| R-49 | **History / intraday routing lane** — a matrix lane that can REDIRECT history/intraday | R-42 §9-6 ruling (owner 2026-07-18) | **Filed BY R-42.** R-42 rules intraday rides the **active provider only** (an `intraday: bool` capability gates it honestly). The routing matrix today can **block** a history fetch (→ cache-only) but not **redirect** one — history calls `get_provider().get_history(...)`, the global active provider (`market.py:41-59,616`); the matrix is a **quotes-lane** authority (AMENDMENT-A scope). A second (history/intraday) matrix lane that can redirect history to a non-active capable provider is a **different shape** and its own milestone (extends AMENDMENT-A). **Plan-file gate standard.** Cross-ref: intraday-series.md §9-6, §2.6. | parked |

---

## Themes

### v2.1 — "accounting precision" (D-088)

The first coherent post-v2 milestone bundles three parked items that together
sharpen the ledger's accounting fidelity. They are grouped for sequencing only —
**each still requires its own plan file** before any is built, and each keeps its
individual gate:

| In the theme | Item | Individual gate (unchanged) |
|---|---|---|
| R-6 | `spec` (specific-lot) cost-basis method | extends the per-account fifo/average selector |
| R-7 | Corporate actions — de-merger / spin-off + ticker rename | **owner-identified 2026-07-10**; plan-file coverage below |
| R-8 | Historical FX series (trade-date backfill + per-date realised totals) | native currency stays filing-grade; base totals stay explicitly indicative |
| R-14 | FD accrued-interest valuation | plan file must cover day-count, compounding, maturity, and provenance labelling of calculated values |

**R-7 — de-merger / spin-off + ticker rename (owner-identified 2026-07-10).**
A de-merger/spin-off is a **merger-in-reverse**: one holding splits into several
resulting instruments. The plan file **must cover**:

- **Cost-basis apportionment** across the resulting instruments per an **approved
  ratio**, with **holding-period carry** (each resulting lot keeps the original
  acquisition date) and **zero realised gain** (a spin-off is not a sale) —
  mirroring the merger invariant in reverse.
- **Ratio as user input vs a published-ratio reference** — how the apportionment
  ratio is entered/sourced and labelled (user-entered vs referenced).
- **Multi-instrument creation flow** — recording one action that creates/updates
  several instruments (the parent + each spun-off child) in one reviewed step.
- **FX acquisition-rate carry** — each carried lot preserves its original
  acquisition FX rate (so trade-date-FX realised totals stay correct downstream).
- **Provenance labelling** — the resulting synthetic lots are marked as
  corporate-action-derived (source/valuation labelling), never presented as market
  buys.
- **Ticker / symbol rename** — expose editing an instrument's **symbol** (today
  `InstrumentPatch` edits name/class/country/source only). **Verified 2026-07-10:**
  the data model already supports this safely — transactions/holdings/lots
  reference `instruments.id` (FK), and instrument identity is `(id_type, value)`
  in `instrument_identifiers`, so a rename preserves history and price continuity;
  only the **API/UI to change the symbol** is missing. (Instrument **name** edits
  are already supported — see GLOSSARY "Ticker / name change".)

All other R-items remain independent parked breadcrumbs, not part of this theme.

---

## Not on this list (recorded, but not ROADMAP)

- **SaaS/PaaS hardening layer** (D-001) — a future proprietary layer, recorded
  in an ADR rather than here. v2 core must not make choices that preclude it,
  but it is out of scope for v2 core and is not a parked ROADMAP item.
