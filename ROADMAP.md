# ROADMAP.md — LedgerFrame Parked Items

**The rule: nothing on this list is built without a plan file in `docs/plans/`.**
These are accumulated breadcrumbs from the v2 decision audit — deliberately *not*
v2 work. Each entry is parked until a plan file covering it exists. Some carry
explicit gates or conditions (below) that a plan file must additionally satisfy.
Source: `docs/audit/DECISIONS.md` (D-001–D-080). Entries mirror that file's
merges — the historical-FX item is a single merged entry (D-020 + D-076).

---

| # | Item | Origin | Conditions / gates | Status |
|---|------|--------|--------------------|--------|
| R-1 | Optional passphrase mode (8–64 chars) | D-002 | — (PIN remains an access lock, not data-at-rest protection) | parked |
| R-2 | User-requestable transaction currencies | D-006 | Must be FX-validated — a currency may exist in the master only if the FX service can translate it | parked |
| R-3 | Domicile for fund-tax display | D-007 | Reintroduces a domicile field beyond the single authoritative `listing_country` | parked |
| R-4 | Instrument notes | D-015 | Follows the drop of the general `Note` table; per-record note fields cover the current need | parked |
| R-5 | Opt-in AI chat history | D-016 | Opt-in only; default remains ephemeral (Product Guarantee 6 — no stored AI conversations) | parked |
| R-6 | `spec` (specific-lot) cost-basis method | D-018 | Extends the per-account fifo/average selector | parked |
| R-7 | Corporate actions — de-merger / spin-off + ticker/symbol rename (v2.1 "accounting precision") | D-019 | **Owner-identified 2026-07-10.** De-merger/spin-off support + exposing a symbol/ticker rename. Splits/bonuses/mergers already engine-covered; see the v2.1 theme for the plan-file coverage. | parked |
| R-8 | Historical FX series (enables trade-date backfill + per-date realised totals) | D-020, D-076 | Merged entry. Native-currency stays the filing-grade output; base-currency realised totals stay explicitly indicative with caveats preserved. **⚡ IN-SCOPE with R-43 by owner ruling 2026-07-18 (RD-9 Amendment 6)** — R-43 (historical valuation backfill) **hard-depends** on per-date FX, so R-8 is pulled into v2.0.0 with it. Cross-ref: release-readiness.md RD-9 Amendment 6; R-43. | ⚡ v2.0.0 scope with R-43 (RD-9 Amendment 6) |
| R-9 | Insurance cash value: opt-in inclusion in Net worth | D-039 | Opt-in only; default remains permanently excluded in v2, stated visibly on Insurance and Net worth pages | parked |
| R-10 | App-wide Detail level | D-040 | Gated on per-page specs (v2 scopes Detail level to Home only) | parked |
| R-11 | User-defined scenario shocks | D-058 | Gated on a proper plan file; "scenario, never a forecast" preserved. **⊕ SEAM NAMED (2026-07-15, page-scenarios §9-7):** the fixed-shock magnitudes are now **named constants with rationale** (`EQUITY_SHOCKS`, `RISK_SHOCK`, `CRYPTO_SHOCK`, `PROPERTY_SHOCK`, `FX_SHOCK` in `services/scenarios.py`) — so this becomes a **config change over an existing seam, not a rewrite**. **⊕ UPGRADED (2026-07-15, page-scenarios §12sc1-2):** the target shape is **interactive sensitivity analysis** — a **per asset-class / currency / region shock SLIDER** over a **PARAMETERIZED scenario endpoint** (the server computes the impact; **no client money math** — D-105); **debounced** requests; the **D-058 copy bar applies to the exploratory surface** (a scenario, never a forecast, even while dragging). **Enabled by** the §9-7 named-constant seam and the §9-4 single-derivation exposures. **Requires its own plan file.** | parked |
| R-12 | Revisit AI validator strictness | D-070 | Only if fallback frequency proves high in practice; the validation contract may not weaken (Product Guarantee 7) | parked |
| R-13 | Per-lane provider priority editing | D-072 | Only on demonstrated need; no user-editable provider priority in v2 (visibility yes, editability no). **⊕ AFFIRMED STILL PARKED (2026-07-18, data-feed-routing §9-R):** R-38 (provider routing matrix) is a **different shape** — a cell selects *one provider per class×country*; it does **NOT** reorder a lane's priority chain and does **NOT** unpark R-13. Lane-priority editing remains parked; Pricing Health stays read-only (D-072). | parked |
| R-14 | FD accrued-interest valuation | D-073 | First post-v2 feature. Plan file **must cover day-count conventions, compounding variants, maturity handling, and provenance labelling of calculated values**. `calculated_accrual` retained in the ValuationMethod vocabulary but no v2 lane emits it | parked |
| R-15 | User-configurable review thresholds | D-084 | The v2 defaults are owner-set (D-084: runway 3 months, goal 180 days, the rest as audited); this makes them user-editable. Each still a named constant with a rationale | parked |
| R-16 | Ticker scroll speed as a per-device display setting | D-047-amend / D-078 | **Owner-identified 2026-07-11 (page-chrome §11-18).** The ticker scroll duration (+ height/density) are tokens today; this exposes scroll speed as a **per-device** display axis (D-078, localStorage — like theme/density), for the **future Settings page plan**. NOT built now; default speed owner-ratified at 30s | parked |
| R-17 | Ticker index entries link to Markets | D-047-amend / D-098 | **✅ SHIPPED 2026-07-13 with the Markets build (page-markets ND-5, §12mk3-3; page-chrome §11-19 CLOSED).** `fetchTickerQuotes` sets the index `href` → `/markets` (holdings still → `/instrument/{symbol}`, D-098); owner verified the index-click lands on Markets live. Unit test (`chrome.test.ts`) pins the mapping | **DONE** |
| R-18 | D-101 themed scrollbar-thumb polish | D-101 | Parked cosmetic from Instrument Detail (the themed thumb didn't paint in **headless captures**; the structural fix — header-owns-gutter — is verified). **NOT addressed in page-chrome batches 1–4** (no scrollbar CSS touched). Named home for the polish/verification; a real browser is now available (ADR-0004 chromium) to confirm the thumb paints and close it | parked |
| R-19 | Customisable Home — movable/resizable widgets, user-selected set | D-046 (amends) / owner 2026-07-11 | **Amends D-046** (Home was deliberately simplified to a fixed set) — the plan file is a **decision amendment, not an addition**. Gates: the widget catalogue = **canonical-page summary readers ONLY** (P-1 + the enforcement corollary apply **per widget** — no widget adds a figure its canonical page lacks); the **D-046 fixed set stays the default + reset target**; **rotation / wall-appliance behaviour defined for custom layouts**. **v2 Home still builds to D-046 unchanged.** | parked |
| R-20 | External widget API — scoped long-lived keys for read-only summary widgets (Home Assistant / MagicMirror / kiosk) | D-069 / owner 2026-07-11 | Builds on **D-069 token semantics** (create / name / revoke; shown once; revoke severs). Gates: **read-only ONLY — no write scope exists**; **per-key scoping**; operates within the **D-001 posture** (LAN/VPN reach, never internet exposure; the plan file **must address the key-over-LAN threat model given no TLS in v2**); **inbound-serving vs no-egress semantics stated explicitly**. | parked |
| R-21 | External-connectivity adaptors (FIX, ISO 20022) as new **provider lanes** in the existing provider/routing model | Product Guarantee 1 & 5 / owner 2026-07-11 | Gates: **FIX = market-data / drop-copy READ-ONLY** — no order flow, no order endpoints (**Product Guarantee 1, protected**); **ISO 20022 = statement / position ingestion only**; adaptors **inherit provenance labelling**; under **no-egress every adaptor makes zero calls** and freshness degrades to **honest stale-flagging (Guarantee 5)**. **One plan file per protocol.** | parked |
| R-21a | MCP surface — recorded separately | D-067 / D-068 / owner 2026-07-11 | **AI-tool integration**, kin to the **AI-surfaces milestone** (D-067/D-068 pipeline), **not a market-data lane** — evaluate there, not as an R-21 provider adaptor. | parked |
| R-22 | AI provider configuration — local (Ollama) vs cloud API | D-067 / D-068 / D-069 / owner 2026-07-11 | **Explicit input to the AI-surfaces milestone plan** (D-067/D-068), not a standalone build. Gates: **API-key handling via the D-069 token pattern** (create / name / revoke; shown once; never returned); ~~**no-egress interaction is normative — under no-egress AI is local-only (Ollama), a cloud provider makes zero calls**~~ **⊕ AMENDED 2026-07-20 (owner, AI-surfaces §9-BIS, option (b)): NO-EGRESS MEANS ZERO CALLS INCLUDING LOOPBACK.** The "local-only" clause is superseded — the shipped gate never implemented it (`app/core/egress.py:82-83` checks the toggle before it looks at any URL; `tests/integration/test_egress_guard.py:120` pins the block at a **loopback** base URL). *Owner's rationale:* a loopback exemption **delegates the promise to a process LedgerFrame does not control** — a local Ollama server makes its own outbound calls, **model pull being the counterexample** — so Commitment 5's *zero outbound network calls* would stop being an observable property of the device. **Only IN-PROCESS inference could reopen it**; a loopback allow-list cannot. Consequence: **two** no-egress posture states, not three — local-provider and no-provider are the same state (AI off, deterministic answers). Full note at `DECISIONS.md` R-22 AMENDMENT; the plan **decides whether first-run/Settings gain an AI configuration step there** (not this first-run milestone — D-045 stays the five steps, P-7). | parked |
| R-23 | RSS / user-defined news feeds as a **news provider lane** | owner 2026-07-11 | A user-defined feed lane in the existing provider/routing model. Gates: **provenance labelling** — each item carries its feed source, never presented as first-party; **zero calls under no-egress** (the lane degrades to honest stale/empty, Guarantee 5), like every other provider lane. | parked |
| R-24 | First-boot **license acceptance gate** | owner 2026-07-11 | **NOTE: cannot be a D-045 checklist step** — skippable ≠ acceptance. Would be a **separate blocking gate** (mounts before/around the shell, not the skippable first-run overlay). **Revisit only if the licensing / distribution posture demands it.** **RESOLVED 2026-07-14 (release-readiness RD-3): NOT needed for the v2.0 SOURCE release — cloning implies reading the LICENSE. R-24 STAYS PARKED.** Its revisit is now bound to a milestone, not a mood: **it is reconsidered if/when PACKAGED binaries ship** (release-readiness RD-1c), because that is the case where the user never sees a repo. *(The owner's earlier 'strike this and build it now' alternative is hereby closed.)* | parked (revisit at **packaged**) |
| R-25 | Personal-profile fields (name, for personalization) | D-045 (amend) / PRODUCT-SPEC / owner 2026-07-11 | **HARD GATES:** requires a **deliberate D-045 / PRODUCT-SPEC no-profiling amendment** (the current spec explicitly forbids profiling); **the owner must re-affirm it against the privacy identity** before any build; **DOB / gender remain out** regardless. Not a gap — a deliberately-declined-until-amended item (see page-first-run-checklist §12). | parked |
| R-28 | **Liquid-vs-illiquid net-worth trend decomposition** — a stacked-area Net worth trend split by liquidity rung | owner 2026-07-12 / page-net-worth §12b1-4 | **FORWARD-ONLY + OPT-IN** (mirrors the ND-1 no-backfill honesty ruling). Grounds — verified §12b1-4: `net_worth_snapshots` stores **scalar totals only** and `portfolio_snapshots.detail_json` is **written empty**, so **no historical split exists**. Gates: the worker **persists a liquidity/class split going forward** (snapshot detail — column vs `detail_json`, **shape TBD in the plan file**); **NO BACKFILL** — historical reconstruction needs **historical FX (R-8, parked)** and would **fabricate a split for manual assets** (property/cash have no price series, contra Guarantee 3); a **`PriceChart` stacked-area mode is a DESIGN-SYSTEM §5 amendment gate**; **plan-file gate standard** (nothing built without it). | parked |
| R-29 | **Implicit "seen" state for Review attention items** — a distinct concept from **Mark reviewed** | owner 2026-07-13 / page-review §12rv1-2 | Owner raised auto-recording a review on full-scroll + navigate-away — **DECLINED for v2** (§12rv1-2): (a) `POST /review/log` is **`require_auth` [S]** — a gated write must not fire silently; (b) a recorded review is a **deliberate `ReviewLog` attestation** (GLOSSARY), and scroll-past is not review — auto-attesting violates the trust posture; (c) it would **pollute Review history**. A future "seen" marker is a **separate concept** from Mark-reviewed and **requires its own concept, auth posture, contract delta + state model, and a plan file** (nothing built without it). Distinct from **per-signal acknowledge/dismiss** (ND-5, also ROADMAP). | parked |
| R-30 | **Postgres backend option** | owner 2026-07-14 | **Hosted-tier concern, paired with D-001's proprietary layer.** **SQLite is the appliance-correct choice for v2** — the file-based backup / restore / install story was **ratified at Gate B** (`backup.sh` + the `update.sh` backup gate + a single data dir *are* the recovery model, and they are the model precisely because the database is one file). A Postgres option is a **hosted-tier** need, not an appliance need; a plan file must state what it does to the backup/restore story, since that story is what a stranger installing the appliance actually depends on. | parked |
| R-31 | **Internationalization** — backend string catalog + per-locale GLOSSARY with parity guards | owner 2026-07-14 | **Platform milestone, and it is ENABLED BY THE ARCHITECTURE WE ALREADY BUILT:** because every user-facing label, vocabulary and money figure is a **served display string** (D-005 frontend-zero-copy; D-105 backend-formatted money), the strings are already in ONE place — the backend — instead of scattered through the components. Gates: a **backend string catalog** (the served labels become catalog keys); a **per-locale GLOSSARY**, since the hard rule is that every displayed term exists in GLOSSARY with that exact spelling — which must hold **per locale**, not just in English; and **parity guards per locale** (`test_glossary_parity.py` generalised — a locale that ships a term the spec lacks fails, exactly as today). Money/number/date formatting stays **backend-side** (D-105), so locale formatting has one home too. **⊕ LOCALE DATE FORMATTING (added 2026-07-15, page-cash-flow §12cf1 walk):** the owner ruled that **served ISO dates STAND** for now (`2026-09-30`) — **platform-consistent** is worth more than per-page prettiness, and a date format is a **locale** decision, not a page one. Formatting dates therefore lands **here**, once, for every page. | parked |
| R-32 | **Voice — DEFINITION PENDING (owner)** | owner 2026-07-14 (RD-9 Timing Amendment 3) | **Named as a RELEASE-SCOPE GATE on 2026-07-14 — and it is the only item in that gate with NO definition anywhere:** no spec, no decision, no plan file, no prior ROADMAP entry. **It requires an OWNER DEFINITION and its own milestone plan before the release gate can even be EVALUATED against it.** ⚠ **No Voice behaviour has been invented, assumed, or inferred.** *What exists today is NOT a definition:* an optional `voice` extra (**vosk** + **sounddevice**) in `pyproject.toml` and an install-time prompt in `install.sh`. That is a **dependency and an installer option** — it says nothing about what Voice IS, what it does, what it may read, or what it may never do. Gates a definition must set, at minimum: **scope** (dictation? command? read-out?); **the Guarantee-1 line** (the platform never executes trades — a voice surface may not become an instruction channel); **the Guarantee-5 line** (no-egress — is recognition local-only, and does audio ever leave the device?); and whether it rides the **P-6 single AI pipeline** (D-067/D-068) or is a separate lane entirely. **This item blocks the release gate until the owner defines it.** **⟶ ANNOTATION (owner, 2026-07-18, page-settings §14st-2):** the Settings **"AI" tab** was named **"AI", NOT "AI & Voice"** deliberately — naming an undefined surface would invent behaviour. The tab **gains "& Voice" only after this R-32 definition lands** (a rename + whatever Voice controls the definition authorises); until then the AI tab carries the AI-config line only. **⟶ RELEASE SCOPE (owner ruling, 2026-07-18, RD-9 Amendment 4):** Voice is **POST-RELEASE** — it **does NOT gate v2.0.0** (Amendment 3 had named it a release gate; Amendment 4 moves it out). Its **definition remains owed**, and it **gates whichever release ships it**. See `release-readiness.md` RD-9 Amendment 4. | **DEFINITION PENDING (post-release)** |
| R-33 | **Per-entity investment policies** | owner 2026-07-14 (page-policy §9-21) | Today there is **one household policy**: a single active `InvestmentPolicy` row with **no entity FK** on it or `PolicyTarget`. That is why `GET /policy/drift` now **rejects `?entity_id` with a 400** rather than serving a precise-looking, meaningless comparison (an entity's actual allocation measured against a policy that was never its own). Making policy per-entity is a **data-model change** (entity FK + a policy-per-entity resolution rule + a household-vs-entity default), not a query param. Gates: what the **household** view means when entities have their own policies (roll-up? a separate household policy?); which policy **Review** summarises; and the **Reports Pack's** per-entity sections (P-3). | parked |
| R-34 | **Goal surfacing beyond Cash flow** | owner 2026-07-15 (page-cash-flow §12cf1-5) | **Recorded answer:** goals surface **canonically on Cash flow** + **Review's goal-soon signal** — and **nowhere else, for now**. Two candidate homes are named so they are not smuggled in as riders: a **Home widget** (an **R-19** evolution — the customisable-Home work, where the widget catalogue is *canonical-page summary readers only*, P-1) and a **Reports Pack section** (**decided at the Reports plan**, not here). **Deliberate scope, not an omission.** No UI change now. | parked |
| R-35 | **Per-entity planning** (goals / obligations / contributions) | owner 2026-07-15 (page-cash-flow §9-15) | None of the three models has an entity FK and no endpoint accepts `entity_id`, so Cash flow is **household-only by construction**. Making planning per-entity is a **data-model change**, not a query param. **Also covers per-entity SCENARIOS** (page-scenarios §9-8 — `/portfolio/scenarios` rejects `?entity_id` today; per-entity what-ifs land with this). Gates: what the **household** view means once entities have their own records; which set **Review** summarises; and the **Reports Pack's** per-entity sections (P-3). **Scope sketch (page-accounts §14ac-4, owner realization 2026-07-16):** also covers **entity- and institution-level FILTERED VIEWS** over Accounts/Holdings — a filter over the account *attribute* (the account-scoped chip precedent, Amendment G), distinct from the per-entity planning *models* above. Per the §9-8 ruling (entity is an account attribute, not a page filter; `?entity_id` stays dormant with **no** UI caller until this activates), these are **NOT batched** onto page-accounts — a pull-forward is **R-35 activation with its own plan file**. | parked |
| R-36 | **Insurance premiums → Cash flow (derived integration)** | owner-requested 2026-07-16 (page-insurance §14in-3) | **Owner calls the current separation *"disintegrated"*** and wants insurance premiums to flow into Cash flow automatically. **NOT built now — no behaviour invented.** A plan file must decide: **derived read-only line vs suggested entries**; **double-count handling** (premiums a user already entered as obligations); **Cash flow's §0-PROTECTED D-057 invariants untouched** — a derived line must **not** enter the user register or the `once`-exclusion semantics silently (contributions never reduce runway; `once` excluded from recurring burn); **one derivation** — the SAME per-policy annual-equivalent served by §14in-2 (`_annual_premium`), never re-derived; **deletion/lapse semantics** (a lapsed policy's premium must leave the projection); and **which figure Review summarises**. | parked |
| R-37 | **Rotation engine** | D-044 / D-017 / owner 2026-07-18 (page-settings §9-2(b)) | **Parked from page-settings §9-2(b)** — the three write-only allow-list keys (`rotation_seconds`, `rotation_pages`, `focus_page`) were **REMOVED** this milestone (D-078 forbids a key with no consumer; shipping keys without the engine was the defect, and shipping the engine inside Settings was scope creep). The engine and the three keys are **re-added TOGETHER, spec-first**. Scope: a **chrome-level rotation engine** honouring **D-044** (configurable page set + interval; **skips errored/empty pages**, D-044), server-persisted config (D-017 — the config lives in settings rows, not localStorage). **Named prerequisite:** the **per-page health signal** the skip rule needs (a page must be able to report errored/empty so the engine can skip it) — no engine without it. The existing TopBar rotation toggle (`AppShell.tsx`, local-state-only today) is the chrome control this engine wires — **HIDDEN 2026-07-18** (owner-ruled at the Settings Phase-0a gate, dead-affordance principle; page-chrome delta note) and **restored here** when the engine lands. **Owner-ruled at the same gate (page-settings §12 ruling (e)):** when the engine lands, **sweep the served defaults surface (`rotation_seconds` in `GET /settings.defaults`, `settings.py:69`)** — a served read-only default left in place this milestone rather than repeat the §9-2 scope-creep concern. **Named prerequisite:** the per-page health signal the skip rule needs. **Plan-file gate standard** (nothing built without it). | parked |
| R-38 | **Provider routing matrix** — per asset-class × listing-country provider mapping | owner 2026-07-18 (page-settings milestone close) | **⚡ ACTIVATED as the NEXT milestone (owner ruling 2026-07-18) — own plan file `docs/plans/data-feed-routing.md`, the R-35-activation precedent (plan-file-first, verify-first).** The **KEPT machinery** (verify-first, confirmed): `CAPABILITIES` already declares `asset_classes`/`regions` per provider (`05-PROVIDERS-AND-ROUTING.md` §A.2; `app/providers/market/router.py:53`, dataclass `:44-45`); the pure resolver already takes `asset_class`/`asset_subclass`/`listing_country` and validates coverage (§A.7; `router.py:195` `route()`), and a per-instrument `source_override` precedence slot already exists (`router.py:237`). **MISSING (the plan's scope):** the **mapping table** (a new data model — per asset-class × listing-country → provider); its **precedence slot** (between the per-instrument override and the active provider); **capability-validated editing** (a cell may only pick a provider whose `CAPABILITIES` cover that class×country); the **matrix editor UI** — canonical home **Settings → Data feeds** (§14st-1); and how **Pricing Health** surfaces **per-cell provenance** (which provider actually priced each class×country). **NOT built in the Settings milestone** — plan-only kickoff follows the close. | **ACTIVATED (next; plan-only)** |
| R-40 | **Alpha Vantage premium feed expansion** | owner-raised 2026-07-18 (data-feed-routing §9 one-pass) | **DEFINITION OWED by the owner.** Premium-tier AV feeds **beyond the current lanes** (e.g. fundamentals / additional series) MAY serve future platform needs. A plan file must first have the owner name **which** feeds, serving **which** page/surface, before any build. **Input material, NOT a plan source:** the owner-held vendor documentation — the plan cites adapter code (`external.py`), never vendor PDFs (the plan-source discipline). **NOT R-38 scope:** R-38 routes *existing* lanes only; it does not add feeds. **Plan-file gate standard** (nothing built without it). | parked (definition owed) |
| R-39 | **chrome-sidebar-refresh** — the sidebar visual/interaction refresh | owner 2026-07-18 (page-settings §14st-3, re-walk) | **Owner-decision, DEFERRED to its own milestone** (§14st-3, the second re-walk ruling). Sequenced as the **FINAL pre-release milestone** (after data-feed-routing · Help · Legal · AI-surfaces). **Translation constraints recorded now so the milestone inherits them (nothing invented beyond these):** (a) **semantic-only colour** — the refresh honours DESIGN-SYSTEM's semantic-only-colour rule, no decorative palette; (b) **no avatar / account block** — this is a single-user appliance (D-001); the sidebar gains **no** profile/avatar/account affordance; (c) **collapse-to-icon rail rides the EXISTING D-078 sidebar-collapsed setting** — the icon rail is the collapsed state of the already-built per-device sidebar-collapsed toggle, **not** a new persistence axis. **Plan-file gate standard.** | parked (final pre-release) |
| R-41 | **Per-provider credentials** — a write-only key **per market-data provider** | owner-surfaced 2026-07-18 (data-feed-routing §16, R-38 Phase 3b re-walk) | **Surfaced BY R-38.** The routing matrix invites **multiple keyed providers concurrently** (a cell may select any capable provider), but the single shared `LEDGERFRAME_MARKET_API_KEY` slot (`app/api/v1/routes/system.py:140`) **cannot** support that — today the provider table can only honestly show SET on the **one active** keyed provider (data-feed-routing §14 key-slot ruling). **Scope sketch (owner to ratify at plan-time):** per-provider **write-only** key storage; the Settings key control gains a **provider dimension**; the matrix **caveat chip** keys off **per-provider** credentials instead of the shared slot. **Plan-file gate standard.** **⛔ POST-RELEASE by owner ruling 2026-07-18 (RD-9 Amendment 6)** — rationale: **YAGNI** — no felt friction until a second keyed provider is actually in use; the shared slot + active-marker honesty holds until then. Cross-ref: data-feed-routing.md §16; release-readiness.md RD-9 Amendment 6. | post-release (RD-9 Amendment 6) |
| R-42 | **Intraday price series** — sub-daily bars (e.g. 1-min for 1D) | owner-raised 2026-07-18 (data-feed-routing §18, §14dr-7); **ACTIVATED 2026-07-18 (owner definition, data-feed-routing §20 / §14dr-13 re-walk)** | **⚡ ACTIVATED as the milestone immediately after data-feed-routing closes, BEFORE Help (owner ruling 2026-07-18) — own plan file `docs/plans/intraday-series.md` (stub now, R-38 stub pattern).** Surfaced BY §14dr-7 (the disabled 1D/5D). Today every live provider fetches **daily only** (`yahoo.py:209` `"1d"`, `external.py:213` `"daily"`). **Owner DEFINITION (2026-07-18):** intraday series is **(a) tier-aware** — respects the learned `av_tier`; the free tier keeps the honest **disabled** 1D/5D, a premium tier enables it; **(b) USER-TRIGGERED** — an explicit fetch **per instrument / per range**, never a background poll (budget discipline: `alphavantage free ≈ 25 req/day`); **(c) PERSISTED permanently once fetched** — stored + reused, no re-fetch on view. **KEPT machinery:** the `PriceHistory.interval` column (`app/models/__init__.py:325`) is the storage seam. **MISSING (plan scope):** the intraday fetch adapter path (intraday interval, not `"daily"`); range→interval mapping; per-instrument/range trigger + persistence; chart consumption (the dr-7 disabled ranges re-enable here) — all deferred to the plan file. **Plan-file gate standard.** **✅ DONE — closed 2026-07-18** (`intraday-series.md`, walk ledger 3 findings / 1 batch + §15; RATIFICATION §6; the dr-25 carryover FULLY ACCEPTED). Filed R-47 (intraday compaction), R-48 (benchmark on intraday), R-49 (history/intraday routing lane). Cross-ref: data-feed-routing.md §18 dr-7, §20 dr-13. | **DONE (closed 2026-07-18)** |
| R-43 | **Historical valuation backfill** — retrospective portfolio valuation | owner-raised 2026-07-18 (data-feed-routing §18, R-38 Phase 3b re-walk batch 3) | **Owner priority — today's Net worth trend is flat/linear** because only forward snapshots exist (cf. R-28 grounds: `net_worth_snapshots` stores scalar totals only). **Scope:** retrospective portfolio valuation from **price history + transactions + per-date FX** — so it **needs R-8 (historical FX series)**; **Decimal engine** (no client money math, D-105); **snapshot persistence**; powers **Net worth trend + performance**. The **biggest** of the three batch-3 requests. **Plan-file gate standard.** **⚡ RULED INTO v2.0.0 by owner ruling 2026-07-18 (RD-9 Amendment 6)** — rationale: Time-to-Value / first-open credibility (a flat/linear trend undermines the first open); **includes the Net-worth snapshot-now trigger**; **hard-depends on R-8 (historical FX)**, so **R-8 becomes in-scope with it**. Sequencing (architect, owner may override): **immediately after intraday-series (R-42), before Help** — R-42 and R-43 grow the same history store, so the storage/keying decisions are made adjacently. **⊕ SCOPE ADDED at the R-42 close (architect ruling under owner's standing delegation, 2026-07-18, reversible by dated entry): transaction currency + trade-date cost-basis FX is FOLDED INTO R-43.** The R-42 3b walk found India funds recorded cost in **SGD** while NAV is **INR**, distorting unrealised P/L — a **data-entry currency question, not a valuation bug** (R-42 batch-1 correctly did not rewrite recorded costs). Trade-date conversion is R-43's natural home, adjacent to the R-8 historical-FX dependency it already carries. Cross-ref: intraday-series.md MILESTONE CLOSE (STEP 1); data-feed-routing.md §18; release-readiness.md RD-9 Amendment 6; R-8; R-28. | ⚡ v2.0.0 scope (RD-9 Amendment 6) |
| R-44 | **News thumbnails** — instrument / News / Home article images | owner-raised 2026-07-18 (data-feed-routing §18, R-38 Phase 3b re-walk batch 3) | **New egress** — `og:image` / RSS `enclosure` image fetch, so the plan **must** make it a **no-egress-mode conditional** (zero calls under no-egress, honest fallback — Guarantee 5, the provider-lane precedent); **caching** of fetched images; **layout** on the instrument / News / Home surfaces. **Plan-file gate standard.** **⛔ POST-RELEASE by owner ruling 2026-07-18 (RD-9 Amendment 6)** — rationale: **cosmetic**; a **new egress surface** — **can ride the R-45 egress ruling later** (same decision class). Cross-ref: data-feed-routing.md §18; release-readiness.md RD-9 Amendment 6; D-075/D-060 (no-egress symmetry); R-45. | post-release (RD-9 Amendment 6) |
| R-45 | **Per-instrument + default news coverage** | owner-raised 2026-07-18 (data-feed-routing §14dr-29 re-walk) | **AMENDED SCOPE per the dr-29 verification:** three `DEFAULT_FEEDS` **already exist** (`app/services/feeds.py:37-41`) and Home headlines are **holdings-scoped** (`Home.tsx:67`) — an erased instance legitimately shows none (NOT a bug). R-45 is therefore **(i) per-instrument news source** (owner proposal: `finance.yahoo.com/quote/{TICKER}`; whether a stable feed/API exists vs scraping is an **OPEN QUESTION — do not assume**) and **(ii) default-coverage expansion** so typical holdings actually yield headlines. **Egress ruling required** (P-8 symmetry, no-egress conditional — same decision class as R-44; **take the two rulings together**). News is a **closed page** → dated delta notes + pre-pass re-runs on touch. **Plan-file gate standard.** **⚡ PULLED INTO v2.0.0 by owner ruling 2026-07-18 (RD-9 Amendment 5).** **⊕ VERIFICATION ITEM (observed 2026-07-18, R-42 close):** the **Home holdings-scoped headlines vs per-ticker feed inconsistency** — confirm/resolve in the R-45 walk. Cross-ref: data-feed-routing.md §14dr-29; R-44. **⊕ SCOPE ADDITION (owner, 2026-07-20, AI-surfaces §9(b) one-pass) — R-45 NOW OWNS D-068's "richer AI narration" of the Briefing.** The AI-surfaces milestone ships the Ask panel and the instrument explainer; **D-068's optional validated narration is DEFERRED HERE** and does **not** ship in AI-surfaces. Rationale: narration changes the **output of an accepted page** (News is D-068's canonical home, `IA:277-282`), and R-45 is already the open ruling on News content and its egress posture — the two belong in one walk rather than one milestone changing News's output and another changing its sources. Binding when it lands: **the model may add no numbers** (D-068 verbatim, `DECISIONS.md:358`); it rides **P-6**, no second model path; validated-before-display. **NO AFFORDANCE ships for it in the meantime** (dead-affordance rule, page-settings §12 (d)) — `frontend/src/api/news.ts:3-4` keeps its served-deterministic-string note, and `GLOSSARY.md:177` **Briefing** keeps its forward reference, whose "future addition (AI-surfaces milestone, D-068)" wording now resolves to **this row** and is corrected when narration ships. Cross-ref: ai-surfaces.md §9-CLOSED (b). | parked (v2.0.0 scope) |
| R-46 | **Home summary cards (post-close pages)** | owner-raised 2026-07-18 (data-feed-routing close) | Elevate Home with summaries from pages built **after** it: **(i)** Cash-flow runway, **(ii)** liquidity what-ifs (Scenarios), **(iii)** upcoming insurance renewals, **(iv)** goal progress (Cash flow). **Rationale:** pages built after Home should elevate Home. **Constraints:** Home is **closed/accepted** (dated delta notes + pre-pass re-runs); **summarise-with-reader ONLY** per the IA ownership tables — **no re-derivation** (P-1 + the enforcement corollary per card, the R-19 precedent); **D-046/D-047 Simple/Full** composition; **ratified components only**. **Plan-file gate standard.** **⚡ PULLED INTO v2.0.0 by owner ruling 2026-07-18 (RD-9 Amendment 5).** Sequencing suggestion (architect, owner to rule): **adjacent to R-39**. Cross-ref: R-19; R-34. | parked (v2.0.0 scope) |
| R-47 | **Intraday compaction / downsampling** — an honest release valve for intraday-row growth | R-42 §9-1 ruling (owner 2026-07-18) | **Filed BY R-42.** R-42 rules intraday retention is **truly forever — no prune, ever** (a guarantee that silently prunes is not a guarantee). The honest growth math (≈390 candles/instrument/trading-day for 1D 1-min; user-triggered per range, so only viewed windows persist) is bounded in practice, but a heavy long-lived instance could still accumulate. **Scope (owner to ratify at plan-time):** downsample or archive **older** intraday windows to a coarser granularity (never delete a window the served series still claims to hold at its stated interval), or a documented keep-last-K-fetched-days policy — **manage bloat by roadmap, never by silent pruning**; the retention guarantee and the served series' honesty about its granularity stay unbroken. **Plan-file gate standard.** Cross-ref: intraday-series.md §9-1. | parked |
| R-48 | **Benchmark on intraday ranges** — benchmark overlay for 1D/5D on Instrument Detail | R-42 §9-5 ruling (owner 2026-07-18) | **Filed BY R-42.** R-42 is **Instrument-Detail-only** and **hides** the benchmark overlay on 1D/5D with a served reason (**"Benchmark comparison is daily-range only"**) — the benchmark rides a daily series (`analytics.py:238-242`) and no intraday benchmark join exists. **Scope:** an intraday benchmark series + a **tz-aligned** per-bar join so 1D/5D can draw the two-line comparison honestly. **Plan-file gate standard.** Cross-ref: intraday-series.md §9-5. | parked |
| R-49 | **History / intraday routing lane** — a matrix lane that can REDIRECT history/intraday | R-42 §9-6 ruling (owner 2026-07-18) | **Filed BY R-42.** R-42 rules intraday rides the **active provider only** (an `intraday: bool` capability gates it honestly). The routing matrix today can **block** a history fetch (→ cache-only) but not **redirect** one — history calls `get_provider().get_history(...)`, the global active provider (`market.py:41-59,616`); the matrix is a **quotes-lane** authority (AMENDMENT-A scope). A second (history/intraday) matrix lane that can redirect history to a non-active capable provider is a **different shape** and its own milestone (extends AMENDMENT-A). **Plan-file gate standard.** Cross-ref: intraday-series.md §9-6, §2.6. | parked |
| R-50 | **AMFI history without the whole-market download** — a cached window store or a per-scheme source | R-43 §20-R13 (2026-07-19) | **Filed BY R-43.** AMFI's history report **cannot be filtered to a scheme code**, so every window is a **whole-market download** — the owner measured **70,458,459 bytes for a single window** (§20-1). R-43's R13 fix (windows begin at **the instrument's own** first transaction, not the book's) already cut the waste **~6×** — fund 145834 was pulling 31 windows back to 2019 for a holding first bought 2025-06-25 — but a multi-fund multi-year backfill still moves hundreds of MB to extract a few hundred NAV rows. **Scope (owner to ratify at plan-time):** a **cached window store** shared across instruments (one download serves every scheme in that window, which is the natural shape given the payload is whole-market anyway), and/or a **per-scheme source** if one exists — **an OPEN QUESTION, do not assume**. Also folds in **§21-3** (a `TimeoutError` escaping the chunk loop costs the fund its remaining windows) if that ruling has not landed sooner. **Plan-file gate standard.** **⛔ POST-RELEASE — architect ruling under the owner's standing delegation, 2026-07-19:** the ~6× cut already landed, and a **backfill is a rare appliance event** — the residual cost does not earn a pre-release milestone. Cross-ref: r43-historical-backfill.md §20-R13, §21-3. | post-release (R-43 close ruling) |
| R-51 | **Help catalogue — the ~44 `[Help]`-marked terms with no entry** | page-help §9-5 (2026-07-19) | **Filed BY the Help milestone.** `GLOSSARY.md`'s preamble promises that *"terms marked **[Help]** have a full what/why/improves entry in the in-app Help catalogue"* — **73 terms carry the mark; the catalogue serves 29.** The gap is **≈44 entries** (an upper bound: some `[Help]` marks are **PROPOSED**, not RATIFIED, so the genuinely-owed count is lower and must be counted, not assumed, at plan time). **Scope:** author the missing what/why/improves triads, and reconcile the `[Help]` marks that were never ratified. **⛔ POST-RELEASE — architect ruling under the owner's standing delegation, 2026-07-19 (page-help §9-5 Tier 3).** The governing principle, recorded verbatim at the ruling: ***"describing itself falsely is a release bar; describing itself partially is not."*** Tier 1+2 (every false entry corrected, every built page documented) shipped in the Help milestone and IS release-blocking; completeness is not. **Visibility mechanism so this does not go quiet:** the parity guard reports the marked-but-unserved count as a non-blocking number. **⟶ MEASURED (2026-07-19, counter built `87d53d1`; page-help §9-bis-8c):** the promised visibility mechanism now exists and its **first reading supersedes the ≈44 estimate above** — **71 `[Help]`-marked · 29 served · 58 marked-but-unserved, reported as an UPPER BOUND**. The bound is not 58 owed entries: it is inflated by a **multi-term-cell name-shape artifact** (one GLOSSARY row can carry several terms in a single bolded cell while `help.py` serves them as separate entries, so a title-join scores served terms as unserved), and further by still-unratified **PROPOSED** `[Help]` marks, which were never a promise. R-51's instruction — *"must be counted, not assumed"* — stands; the counter keeps the number in view, it does not adjudicate it. **Status unchanged: POST-RELEASE.** **Plan-file gate standard.** Cross-ref: page-help §9-2, §9-5, §9-bis-8c. | post-release (Help close ruling) |
| R-52 | **`Total value` — a D-021-retired term still shipping in live UI copy** *(filed as "a Portfolio stat label" — see the premise correction)* | page-help Phase 0 delta 3 (2026-07-19) | **Found BY the Help content pass, NOT fixed by it.** `GLOSSARY.md:327` retires **"Total value"** under **D-021** (*"use Net worth (with liabilities) / Gross assets (positive holdings), per context"*); the deprecated table says outright *"These must not appear in UI copy."* **Scope as executed:** rename the served label to **Gross assets**, grep app-wide (label changes are app-wide, page-chrome §11-4), dated delta notes, re-run the pre-pass. **⚠ PREMISE CORRECTION (2026-07-19, recorded not quietly fixed):** this row was filed — and ruled — on the premise that the term rendered as a **Portfolio** stat label. **It did not.** `key_stats().metrics` reaches exactly one frontend consumer, `Portfolio.tsx:318`, which filters to `term_id === "term-concentration"`, so Portfolio never rendered it; the genuinely user-visible instance was the **Reports Pack** per-entity Net-worth card (`reports_pack.py:248`), which the app-wide grep is what found. Both sites corrected. `tests/integration/test_performance.py` had **asserted** the retired term — the second test this milestone found pinning a defect in place. *The pre-pass exists to check the claim, not to confirm it.* Cross-ref: page-help §9-7; delta notes in `page-portfolio.md` + `reports-pack.md`. | **✅ RESOLVED pre-release** — owner ruling 2026-07-19, commit `cc2daab` |
| R-53 | **Personalised glossary examples via engine-served derivation traces** | page-help §9-bis-3 (2026-07-19) | **Filed BY the Help redesign.** Help's Section-3 glossary ships **static, sample-marked** worked examples (§9-bis-3). Showing the user's **own** figures with a **step-by-step derivation** is a materially different thing: it requires the **calculation engine to SERVE derivation traces** — a **new API surface and a contract change** — because the alternative, re-deriving the number in the frontend to narrate it, **violates the one-derivation law**. A Help page that recomputed a figure to explain it would become a **second derivation site for that figure**, which is exactly the failure the law exists to prevent, and it would be free to drift from the canonical page while looking authoritative. This is therefore **a distinct architectural epic, not a UI rider on the Help milestone** — the work is in the engine, not the page. **Scope (owner to ratify at plan-time):** a trace-serving shape (which metrics, how a trace is represented, how it stays in step with the engine that produced it), the contract delta, and the Help rendering that consumes it. **⛔ POST-RELEASE (owner ruling 2026-07-19).** **Plan-file gate standard.** Cross-ref: page-help §9-bis-3; the one-derivation law; R-51. | post-release (owner ruling 2026-07-19) |
| R-54 | **Deterministic answer intelligence: the two-tier Ask panel** | Owner-raised 2026-07-20, **by using the live panel** (ai-surfaces Phase 0a) | **Origin — found by use, not by review.** The owner used the shipped Ask panel and identified that **most of its value needs NO model at all**: intent routing over canonical data the device already holds. His framing: *"use-case based intelligence achievable without any LLM,"* with the explicit product value that *"a clear call-out of what the inbuilt AI can do without egress is great value in itself, with the ethos of Privacy as the core of the solution."* **Architecture — the panel becomes TWO HONEST TIERS. Tier 1 — deterministic:** intent routing + canonical endpoints; makes **zero network calls BY CONSTRUCTION**, therefore works in **every posture including no-egress**, and no egress question can even arise. **Tier 2 — model narration:** egress-gated per the **R-22 amendment** (option (b), DECISIONS.md). **Under no-egress the panel goes LOCAL, not dark.** **Tier-1 categories (the owner's examples — illustrative, NOT exhaustive):** **(a) term/knowledge** — *"what is XIRR"* retrieves the **user's own calculated XIRR** from its canonical endpoint **alongside** the glossary explanation, and this pattern applies to **any term with a live figure**; **(b) action** — *"how do I add a holding"* answers with the Help steps **plus a deep link to the actual add form**; **(c) navigation/settings** — *"toggle the theme"* deep-links straight to the Settings tab/control. **Boundaries:** the panel **EXPLAINS AND POINTS; the page ACTS** — no inline controls inside the panel (deep links, never embedded toggles). **Step-by-step calculation display is R-53** (engine-served derivation traces) — **cross-ref, do not duplicate**; together **R-53 + R-54 make the panel useful with the model as garnish, not engine** — the correct architecture for a trust product. **Posture copy:** R-54 **owns the posture-copy amendment** when tier-1 formally lands (dated notes on the ratified strings; the accuracy guards hold **both versions true in their time**). **NOTE:** the no-egress **deterministic answering that shipped at this 0a is tier-1's SEED** — see ai-surfaces §12-3; the shipped banner string is its first artifact. **Process:** own plan file, **survey-first**, §9 one-pass in chat, full loop (**0a by looking**, 3b, close under the **Help Currency Law**). **⚡ PULLED PRE-RELEASE (RD-9 Amendment 7, owner 2026-07-20).** Cross-ref: ai-surfaces §12-3, §13; R-53; R-22 amendment (DECISIONS.md); release-readiness RD-9 Amendment 7. | ⚡ v2.0.0 scope (RD-9 Amendment 7) **CARRIED INTO R-54 (chat ruling 2026-07-20):** (i) **the contention-robustness fix** for `tests/integration/test_ai_facts_routing.py:34` — re-assigned from AI-surfaces post-close (ai-surfaces §19-K); enters R-54's ledger as an intake row per the TEMPLATE amendment; (ii) **fixture hygiene** — `frontend/src/components/ui/AskPanel.test.tsx:27` mocks the served `privacy_label` with a *retired real string*; make it obviously synthetic (e.g. `"POSTURE LINE (TEST FIXTURE)"`) so a grep or a specimen can never mistake fixture copy for served copy; (iii) **posture-descriptor unification as a §9 item** — the Ask posture line says *"OpenAI-compatible endpoint"* while the Settings tab/GLOSSARY label says *"Ollama-compatible"*; both ratified, both true; whether to unify is decided at R-54's §9, which owns posture copy. **⚠ 2026-07-20 correction (r54 §0-K):** *"(ii) retired real string"* **was wrong** — the fixture string is **LIVE** (`POSTURE_LOCAL_NPU`, `app/api/v1/routes/ai.py:61`, in the pinned five-string table, ai-surfaces §12-3). "hailo" was retired from `kind_label`, a **different served field**; the Hailo/Ollama `privacy_label` was ratified **later**, at the 0a look. The hazard is **byte-identity with served copy**, and a **second instance exists** (`NO_EGRESS_STATUS`, `AskPanel.test.tsx:37-39`). **Resolved as §9-H.** |
| R-55 | **Help content: asset classes & corporate actions** | Owner-raised 2026-07-20 (ai-surfaces Phase 0a) | **Origin.** The owner identified **missing Help coverage** in two areas: **the asset classes** (what each class *is*; how to **add / delete / modify** each, with the **class-particular parameters**) and **corporate actions** (what they are; how to apply **each kind** to holdings). **⛔ HARD GATE — VERIFY-FIRST:** R-55 documents **ONLY what shipped code actually does**. The **accuracy guards enforce this mechanically** — an entry describing **unbuilt** behaviour goes **RED, correctly**. The survey must state **honestly** which of the owner's list is **CONTENT DEBT** (feature exists, Help silent) versus **FEATURE ABSENCE** — and a feature absence becomes **a NEW ROADMAP ROW, never Help copy**. **The density concern (owner's words):** *"help is going to get dense every day; there has to be a subtle and easy way to handle this."* **Partly answered already** by structure — the three-section journey + type-ahead search scale, and the **Help Currency Law** keeps content **true** as it grows. **The OPEN QUESTION R-55 carries** is therefore not density but **content INTAKE**: a lightweight way to notice *"this feature has no entry"* **beyond** the built-page-without-entry guard, which only sees pages. **Plan-file gate standard.** **⚡ PULLED PRE-RELEASE (RD-9 Amendment 7, owner 2026-07-20).** Cross-ref: page-help §9-2, §9-5, §9-bis-8c; R-51 (the marked-but-unserved catalogue gap — **distinct**: R-51 is glossary `[Help]` parity, R-55 is subject coverage); release-readiness RD-9 Amendment 7. | ⚡ v2.0.0 scope (RD-9 Amendment 7) **CARRIES THE §19-J GUARD (chat ruling 2026-07-20):** R-55 ships the **findability parity guard** — for every sanctioned short form in the GLOSSARY parity set, Help search on the short form returns its owning entry (measuring the store against the *reader*, which spec↔code parity does not). Cross-ref ai-surfaces §19-J. **⊕ W-1 DEIXIS (R-54 0a-i, owner 2026-07-21):** R-55's content gate **inherits the named-deixis rule** — canonical-page deixis is NAMED (*"on the Markets page"*), never pointed (*"here"*), because **every Help body is now also Ask-panel copy** (R-54's widened fact pack), so page-relative deixis is false when read in the panel. Cross-ref r54 §9-I. |
| R-56 | **A legitimately ZERO figure can never be narrated** — `_sig3("0.00") → ""` is discarded, so a true zero traces to nothing | ai-surfaces §12-4, **ruled as Finding 7** by the owner 2026-07-20 | **Found while BUILDING the narration stub, not by review** — the first three attempts to photograph a passing narrated answer fell back with `unsupported figure '0.00' not in the facts`. **Mechanism, exactly:** `app/ai/safety.py:93` `_sig3()` returns the first three **significant** digits, so `"0.00"` reduces to the empty string; `safety.py:133` then does `fact_sigs.discard("")`, removing it from the traceable set. A **true and useful** zero — *"Today's change: 0.00 SGD"*, *"Income: 0.00 SGD"* — therefore **traces to no fact**, and any answer that quotes it **fails validation clause 2** and falls back to the deterministic template. **⚠ IT ERRS SAFE, and that is why it is polish and not a defect:** the failure mode is *a zero goes un-narrated*, never *a zero is fabricated*. The user still sees the figure — it is in the fact pack, correctly formatted, above the answer; only the prose cannot mention it. **The product is quieter than it could be, never wronger.** **Why the fix is a JUDGEMENT and not a typo:** under a leading-significant-digits comparison a zero is genuinely indistinguishable from "no significant digits", so the discard is not a mistake — it is the correct handling of an empty parse, applied to a value that happens to parse empty legitimately. Any repair must distinguish *"this token had no digits"* from *"this token's digits are all zero"* **without** widening what the validator will accept, and the contract **may not weaken** (Commitment 7 / D-071). **Scope sketch (owner to ratify at plan-time):** a zero-aware signature (e.g. a sentinel for a numerically-zero token) covering the token side and the fact side symmetrically; guards asserting a zero fact **can** be cited **and** that no non-zero figure becomes citable as a side effect. **⛔ POST-RELEASE by owner ruling 2026-07-20 (Finding 7).** **Plan-file gate standard.** Cross-ref: ai-surfaces §12-4, §14-5; SECURITY-BASELINE §5 clause 2; D-071. | post-release (owner ruling 2026-07-20) **⊕ 2026-07-21 cross-ref (R-54 F-3, `33f57bf`):** F-3's fix removes **rendering-artifact zeros** from `_sig3`'s input — a sub-cent price no longer arrives as `"0.00"`, so it no longer reduces to `""` and is no longer discarded from the traceable set. **R-56 remains open for TRUE zero-valued facts** (a genuine `0.00` change, true and useful); its **blast radius is now smaller, not closed.** |
| R-57 | **AI model management (Settings › AI)** — endpoint URL + write-only key + kind selection | owner-ruled 2026-07-20 (chat, AI-surfaces close review) | **Origin.** At the close review the owner asked how a user configures an external OpenAI-compatible endpoint; today the answer is **environment only**, and the tab says so in its ratified sentence (*"Model management is not configurable here yet"*). **Ruled: build the configuration surface, required pre-release.** **Scope sketch (to ratify at plan-time):** the AI tab gains configuration for the **three kinds** (GLOSSARY): kind selection; endpoint URL for On-device model (Ollama-compatible); endpoint + **write-only** API key for External model (R-41's key pattern — write-only, never echoed; §8 secrets discipline: the F9 note already refuses to name values). **The interplay is the hard part:** F9's divergence detection extends — a write while an OS-env override is in force must say it did nothing (ai-surfaces §17-4); `app/ai/vocabulary.py` stays the **one resolver** (Finding 6 — *two sources of truth for one fact is the whole defect*); the *"not configurable here yet"* sentence retires with a **dated note**, both versions true in their time (the posture-copy pattern). **Boundary:** configuration, not management — **no model pull/download UI** (the R-22 amendment rationale: LedgerFrame does not control the model server's own egress). **Plan-file gate standard** (own plan file, survey-first, §9 one-pass, 0a by looking, Help Currency Law — the Settings Help entry changes). Cross-ref: ai-surfaces §17-4, §14-3/F6, GLOSSARY three kinds, R-41, R-54. | ⚡ v2.0.0 scope (RD-9 Amendment 8) |
| R-58 | **The `settings.key` check-then-insert race, everywhere else it lives** — four filed sites outside `get_history_cached` | F10 census, chat ruling 2026-07-20 | **Origin — a census, not a report.** The F10 delta fixed this race at **four sites inside `get_history_cached`** and the ruling required a **read-only sweep of `app/`** for the same shape rather than stopping at the reported defect. The shape is **`SELECT Setting -> if absent, session.add(Setting(...)) -> flush()`** on `settings.key`: two concurrent callers both read absent, both insert, and the loser dies on `sqlite3.IntegrityError: UNIQUE constraint failed: settings.key`. **THE FOUR FILED SITES, in the order they should be taken:** **(1) `app/services/briefing.py:201-207` — FIRST, and it is not a tie.** It is a **generic `_set(session, key, value)` helper**, so the defect is not at one call site but at **every caller it has**, present and future; fixing it retires the widest blast radius in the census and is the one place where a repair compounds. **(2) `app/services/feeds.py:72-78`** (`FEEDS_SETTING_KEY`). **(3) `app/api/v1/routes/settings.py:131-135`** — the settings PATCH loop, user-triggered. **(4) `app/api/v1/routes/system.py:617-621`** — reset writing `SEED_FLAG_KEY`. **ADJACENT, NOT THE SHAPE — recorded so it is not mistaken for a fifth:** `app/seed/demo.py:327` inserts `SEED_FLAG_KEY` **unconditionally, with no SELECT at all**. It is not a check-then-insert race, but a **concurrent double-seed would still collide on it**; whether that path can ever run twice is a question R-58 answers rather than assumes. **⚠ SEVERITY, STATED HONESTLY: none of the four sits on a guaranteed-concurrent path** the way F10's did — F10 was on the fresh-DB first-load path **every new install takes**, which is why it was release-train blocking and this is not. These are reachable under concurrency, not reached by it as a matter of course. **That is a severity statement, not a dismissal:** the same defect that 500'd a first load will 500 these when their path is concurrent, and the repair is now a **known, tested, one-line primitive**. **⛔ FAIL-FIRST IS MANDATORY AND SO IS ITS BLINDNESS PIN (F10's hardest-won lesson, CLAUDE.md *a guard is pinned against going blind*):** each site's test must **reproduce the RED first**, and must **assert it REACHED the insert it guards** — at F10 the site-4 test **passed twice while never reaching the insert at all** (an `alphavantage` stub without `av_tier="premium"` is gated off as `tier_disabled`, `market.py:510`, and the route returns early). **A green test that never arrived is indistinguishable from a green test that found nothing.** Note also F10's posture lesson: a preceding write in the same transaction can take SQLite's write lock and **serialise concurrent callers, hiding the race entirely** — so the fresh/new-entity posture is often the **forgiving** one, not the worst case. **Reproduction and fix verified on SQLite only; Postgres injectable via `LEDGERFRAME_DB_URL` (02-DATA-MODEL §2.2) widens the race window — the savepoint-claim primitive is isolation-portable, but untested there, as is the whole suite.** **The fix is already written:** reuse F10's `_claim_marker` shape (`app/services/market.py`) — insert inside a **SAVEPOINT**, absorb the loser's `IntegrityError`, return whether this caller won — promoted to a shared home rather than copied four times, so the sites cannot drift apart. **Plan-file gate standard.** Cross-ref: `ai-surfaces.md` §17-5 delta note (the census), §18 F10 row, commit `63ec86a`. | ⚡ v2.0.0 scope (RD-9 Amendment 9) |
| R-59 | **URL-addressable entity-creation dialogs — phase 1: the add-holding form** | R-54 §0-F survey; owner-ruled PRE-RELEASE at the §9 one-pass (chat, 2026-07-20) | **Origin.** R-54 tier-1(b) (*"how do I add a holding"* → Help steps **plus a deep link to the actual form**) found the add-holding form is a `useState` modal (`Holdings.tsx:107`, rendered `:527`) — **no route, no `?add=` param**; the link **cannot exist today** (dead-affordance rule: a target the survey finds missing becomes a ROADMAP row, never a link). *Owner's ruling, verbatim:* "Accepted. (Industry best practice: Prioritizing URL-addressability for core transactional flows (Add Holding) pre-release drives critical user value, while deferring generic settings deep-links prevents milestone scope creep)." **Phase 1 (pre-release):** the add-holding dialog becomes **URL-addressable on `/holdings`** (HashRouter param; honours the `setParams` conventions **after** R-54's sibling-param fix, §9-D). **The R-54 link registry gains the form's ID only when the route ships — the bidirectional resolution guard enforces this ordering MECHANICALLY** (until then tier-1(b) links to the **Holdings page**, which exists). Delta-scale, under the **guard-REDs-an-accepted-surface rite**: Holdings is closed, so a **dated delta note in `page-holdings.md` + that page's pre-pass re-run ship in the same delta**. **The general entity-dialog pattern** (import/purge/tags/edit-txn, `Holdings.tsx:108-111`) is **POST-RELEASE and stays in this row.** Cross-ref: `r54-deterministic-answers.md` §0-F, §9-D/§9-E; RD-9 Amendment 10. | ⚡ v2.0.0 scope (RD-9 Amendment 10) |
| R-60 | **Control-level deep linking within Settings tabs** | R-54 §0-F survey; owner-ruled POST-RELEASE at the §9 one-pass (chat, 2026-07-20) | **Origin.** The theme control is **not individually addressable** — only its tab (`Settings.tsx:232-243`; the control reads `useTheme()` at `:221`). **Tab-level addressing is COMPLETE** (all seven tabs incl. AI and About, `Settings.tsx:83-84,107-110`) and is **ruled sufficient for v2.0.0**: landing on the right tab **is** pointing, and the control is on screen on arrival — which is exactly what *the panel explains and points; the page acts* asks for. *Owner's ruling, verbatim:* "Accepted. (Industry best practice: … deferring generic settings deep-links prevents milestone scope creep)." **Scope when taken:** a `?control=` param that focuses/highlights a named control. **Note the prerequisite:** `Settings.tsx:110` calls `setParams({tab:v})` with a fresh object and **drops sibling params** — R-54 §9-D fixes that, so this row inherits a working base. Cross-ref: `r54-deterministic-answers.md` §0-F, §9-D. | post-release |
| R-61 | **Typed AI response models — `/ai/*` contract pinning** | R-54 §3b finding (2026-07-20); owner-ruled POST-RELEASE at the §9 conveyance (chat) | **Origin — found while RESOLVING §3b, not while looking for it.** R-54 asked the narrow question *"does tier-1 need a contract delta?"*, verified the frozen contract, and answered **no — 141 paths / 71 schemas unchanged**. The verification is what surfaced the row: **`ai_facts` is declared `-> dict`** (`app/api/v1/routes/ai.py:26`) and returns a hand-built dictionary (`:33-38`); **`/ai/chat` is a `StreamingResponse`** (`:135-143`). **`GroundingFact` is not a contract schema** — it exists only as a Pydantic model (`app/schemas/ai.py:46-67`) that never reaches the contract because **no route declares it as a `response_model`**. The AI schema set is **`ChatIn` and nothing else**. **The consequence, stated plainly: the served AI response shapes are NOT PINNED, so a field can be added to the fact pack or an SSE event and `make api-contract-check` stays GREEN while the shape the frontend consumes moves underneath it.** *This is the TEMPLATE's `response_model` note* (`TEMPLATE-page-build.md:204-206`) **INVERTED** — that note warns a typed response **strips** an undeclared key; here there is **no model**, so nothing is stripped **and nothing is pinned**, and "141/71 unchanged" would read as *nothing moved* when something did. **Release-relevant coverage already exists and is NOT this row:** R-54 **Phase 0-6 ships a served-shape test** on `/ai/facts` and the `facts`/`provenance` SSE events — the guard that turns red when tier-1 changes what the panel is handed. **This row is the DURABLE fix:** declare `response_model`s across `/ai/*` (and the briefing surfaces), regenerate `API-CONTRACT.json` + `docs/openapi.json`, and let the drift check own these shapes as it owns every other. **Scope note:** it touches **every** AI surface and would change the contract counts, so it is **larger than R-54** and was deliberately **not folded into it** — R-54 recorded the knowledge rather than inventing a ruling. *(Owner, at the §9 conveyance: the finding was too good to live only in a plan file.)* **Plan-file gate standard when taken.** Cross-ref: `r54-deterministic-answers.md` §3b, §7-E; `TEMPLATE-page-build.md:204-206`. | post-release |
| R-63 | **Pricing routing reliability — the chain is fixed once and for all** | Owner ruling 2026-07-23 (chat, at the R-54 close), RD-9 **Amendment 11** | **Origin.** The owner: *"data is the core of this platform, can't leave it so loose"* / *"needs to be fixed once and for all."* Pricing routing today can leave holdings unpriced in ways the product reports as a single flat "none", masking distinct causes. **⚡ PRE-RELEASE.** **INVESTIGATION-FIRST, and it is a hard gate on the fix:** the milestone opens with a **read-only diagnosis on the owner's LIVE instance** — logs + **one instrumented refresh** — to see the real failure distribution before shaping code. **⛔ Never mutate his data; never print keys/secrets** (§8 secrets discipline; the F9 note's refuse-to-name-values pattern). The fix is then shaped by **five recorded survey inputs** (owner's sources: Vibe-Trading review, MIT, *ideas-not-code*): **(a)** a routing **rule pins the chain's HEAD, never removes the fallback net** — so the shipped Settings sentence about routing becomes *true*, not aspirational; **(b)** failure states **named precisely** — `throttled` / `unmapped` / `errored` / `empty` are **not one "none"** — and surfaced in **Pricing Health diagnostics**; **(c)** **per-symbol empty-result honesty** (the `.BSE` suspect — an empty provider result reported as a real "no price" rather than a routing miss); **(d)** a **provider preflight + a "provider doctor"** live-chain test on Pricing Health (does the configured chain actually resolve a known symbol end-to-end?); **(e)** **cache staleness honesty for forming bars** (a still-forming bar is not a settled price). **Plan-file gate standard** (own plan file, survey-first — here literally, the live diagnosis — §9 one-pass, full loop; Help Currency Law — Pricing Health + Settings routing copy will move). Cross-ref: `release-readiness.md` RD-9 Amendment 11; data-feed-routing (R-38); `05-PROVIDERS-AND-ROUTING.md`. | ⚡ v2.0.0 scope (RD-9 Amendment 11) |
| R-64 | **Alpha Vantage capability leverage — one provider used fully** | R-63 §0-E / §9-10 (owner ruling 2026-07-23, §9 one-pass) | **Filed BY R-63 as the (g) scope fence.** R-63 fixes pricing routing on the AV lane; the configured provider serves far more than quotes. **Umbrella row — decomposed into per-capability rows when taken, never before:** `NEWS_SENTIMENT` (FREE — article/ticker/topic sentiment) · **fundamentals** (`OVERVIEW`/`INCOME_STATEMENT`/`BALANCE_SHEET`/`CASH_FLOW`/`EARNINGS`, all FREE) · **corporate actions** (`DIVIDENDS`/`SPLITS`, FREE) · `SYMBOL_SEARCH` (FREE — would sharpen mapping, cross-ref R-63 §9-8). Entitlement labels per the committed reference `docs/reference/alpha_vantage_claude_code_reference.md` (`b88adbe`). **⛔ POST-RELEASE** — one lane fixed well beats scope creep; explicitly OUT of R-63. *Owner (R-63 §9-10, verbatim):* "Accepted. (Industry best practice: Enforcing strict feature freezes for current milestones and aggressively pushing non-critical enhancements to the post-release roadmap)." **Plan-file gate standard when taken.** Cross-ref: `r63-pricing-routing.md` §0-E, §9-10. | post-release |
| R-65 | **Test-suite runtime — measure, then parallelize** | Owner ruling 2026-07-23 (chat, test-suite runtime) | **TEST-INFRA.** The solo suite runs ~16 min (memory: backend-suite-run-mechanics); the near-zero CPU points at wall-clock waits, not compute. **⚑ Phase 1 — a CHEAP SURVEY DELTA (may run any time it does NOT displace R-63 build; slots at the next natural boundary):** (a) **`pytest --durations` census** — where the 16 minutes actually go; (b) **real-sleep debt census** — wall-clock sleeps/waits vs mocked clocks, the likely dominant cost; (c) **`pytest-xdist` feasibility read** against the F-10 `RESET_REGISTRY` census + per-worker DB/tmp/port isolation requirements. **Report the table; NO changes in Phase 1.** **⚑ Phase 2 — SHAPED BY THE SURVEY, QUALITY-INVARIANT BY CONSTRUCTION:** (i) clock-mocking **ONLY for incidental time**; behavioral-time tests keep real clocks (or gain a mocked variant **alongside, never instead**) — the sleep census classifies every site incidental vs behavioral, and **unclassifiable defaults to behavioral**; (ii) xdist, if adopted, is an **INNER-LOOP accelerant only** (per-worker isolation censused and guarded like F-10) — **GATE/CLOSE VERDICTS REMAIN solo, uncontended, ordered AND randomized, unchanged**; (iii) any promotion of parallel runs to **verdict** status requires a **paired-run equivalence baseline** presented to the owner **and a separate chat ruling** — **doubt defaults to the slow path.** **R-63 build proceeds unblocked.** **Plan-file gate standard when taken.** Cross-ref: memory `backend-suite-run-mechanics`, `gate-runs-must-be-solo`; F-10 (`63ec86a`). **⟶ PHASE 1 SURVEY DONE 2026-07-23 (rode the R-63 Phase-2 ordered verdict + static analysis, no dedicated run):** **(a) durations** — full run ~18 min / 2130 tests; NO single sink — the slowest 30 tests sum to **~316s (~29%)** and are almost all **heavy AI fact-pack / performance / tier1 integration** tests (~9–17s each, full portfolio derivation per test) plus the genuine concurrency tests (`test_history_cache_race` 16.7s/12.5s). **(b) real-sleep debt** — **ZERO** wall-clock sleeps in test code; the only `sleep` sites are **already monkeypatched** (`test_yahoo_provider.py` `fake_sleep`); app-code retry backoff (yahoo/eodhd/amfi) is never awaited under test (providers mocked). So the runtime is **NOT sleep-bound** — it is per-test **DB drop+create DDL** (the F-10 `_shared_db_clean_slate`, ~3%/test) × 2130 + heavy integration derivation. Nothing to convert to a mocked clock (no behavioral-vs-incidental sleeps to classify — the census is empty, which is the finding). **(c) xdist feasibility** — **FEASIBLE as an inner-loop accelerant.** Blocker is the **shared DB**: the autouse clean-slate drops+creates ONE DB keyed by URL — N workers would collide, so each worker needs its **own `LEDGERFRAME_DATA_DIR`/DB keyed by `PYTEST_XDIST_WORKER`**. Process globals are ALREADY safe: xdist workers are separate processes (module globals naturally per-worker) and F-10's `RESET_REGISTRY` + census guard keep per-test isolation; migration/db tests already repoint `DATA_DIR` at their own `tmp_path`. Gate/close verdicts **stay solo** (owner ruling). Phase 2 (if taken): per-worker DB isolation + the paired-run equivalence baseline before any promotion to verdict status. **⟶ PHASE 2 RULED READY + SLOTTED (owner 2026-07-24): AFTER the R-63 close, BEFORE R-59** (architect sequencing, reversible). Scope: **per-worker DB/`DATA_DIR` isolation keyed by `PYTEST_XDIST_WORKER`** + an **F-10-style census guard** that per-worker isolation is asserted; xdist config; **inner-loop only** (gate/close verdicts REMAIN solo ordered+randomized — the 16–18 min is the paid price of certainty). **⚠ The per-test DDL-reset cost (survey (b)/(c)) is a POSSIBLE Phase-3 candidate, NOT authorized** — any change to `_shared_db_clean_slate` semantics is its OWN ruled delta with F-10-grade guards. | test-infra (non-blocking; Phase 1 survey DONE, Phase 2 slotted) |
| R-62 | **Thin-history 1Y volatility/return/drawdown render null, not `0.0`** | R-54 F-9 (the F-8 broader-ambiguity survey, 2026-07-22); owner+architect-ruled POST-RELEASE (chat, 2026-07-23) | **Origin — the WIDER half of F-8.** F-8 fixed the fabricated `0.00%` on a perf-series **timeout** (`analytics.py` `key_stats`, `7824327`). Its ruling required surveying whether the **documented** Help ambiguity (*"0.0 can mean 'no data'"*, `GLOSSARY.md:164`, `help.py:1024,1042,1084`) was the same code site — **it is not.** It is `performance_series` fabricating `0.0` for **thin** history (`analytics.py:341` `… if len(daily_returns) > 1 else 0.0`; `:342` `ret_pct`), which feeds a **real-looking `0.0`** into the rendered metric and **survives F-8** (a returned `0.0` is indistinguishable from a genuine zero to `key_stats`). *Owner+architect ruling, verbatim intent:* **DEFERRED, post-release** — *"unlike F-8 this ambiguity is DOCUMENTED — the product tells the truth about it in Help — so it is a quality improvement, not an honesty defect; blast radius (Portfolio figures + ratified Help copy + volatility semantics) outsizes its urgency."* **Scope when taken:** thin-history renders **null → `"—"`/omit** (F-8's established honest shape), and the *"0.0 can mean no data"* **Help copy moves WITH the behavior** (Help Currency Law) — GLOSSARY spec-first. **Reversal:** `F-9 pre-release` (RD-9 amendment) or `F-9 fix now`. **Plan-file/delta gate standard when taken** (rendered-Portfolio-output change on an accepted surface → dated `page-portfolio.md` note + pre-pass). Cross-ref: `r54-deterministic-answers.md` F-9, F-8. | post-release |

---

## Themes

### v2.1 — "accounting precision" (D-088)

The first coherent post-v2 milestone bundles three parked items that together
sharpen the ledger's accounting fidelity. They are grouped for sequencing only —
**each still requires its own plan file** before any is built, and each keeps its
individual gate:

| In the theme | Item | Individual gate (unchanged) |
|---|---|---|
| R-6 | `spec` (specific-lot) cost-basis method | extends the per-account fifo/average selector |
| R-7 | Corporate actions — de-merger / spin-off + ticker rename | **owner-identified 2026-07-10**; plan-file coverage below |
| R-8 | Historical FX series (trade-date backfill + per-date realised totals) | native currency stays filing-grade; base totals stay explicitly indicative |
| R-14 | FD accrued-interest valuation | plan file must cover day-count, compounding, maturity, and provenance labelling of calculated values |

**R-7 — de-merger / spin-off + ticker rename (owner-identified 2026-07-10).**
A de-merger/spin-off is a **merger-in-reverse**: one holding splits into several
resulting instruments. The plan file **must cover**:

- **Cost-basis apportionment** across the resulting instruments per an **approved
  ratio**, with **holding-period carry** (each resulting lot keeps the original
  acquisition date) and **zero realised gain** (a spin-off is not a sale) —
  mirroring the merger invariant in reverse.
- **Ratio as user input vs a published-ratio reference** — how the apportionment
  ratio is entered/sourced and labelled (user-entered vs referenced).
- **Multi-instrument creation flow** — recording one action that creates/updates
  several instruments (the parent + each spun-off child) in one reviewed step.
- **FX acquisition-rate carry** — each carried lot preserves its original
  acquisition FX rate (so trade-date-FX realised totals stay correct downstream).
- **Provenance labelling** — the resulting synthetic lots are marked as
  corporate-action-derived (source/valuation labelling), never presented as market
  buys.
- **Ticker / symbol rename** — expose editing an instrument's **symbol** (today
  `InstrumentPatch` edits name/class/country/source only). **Verified 2026-07-10:**
  the data model already supports this safely — transactions/holdings/lots
  reference `instruments.id` (FK), and instrument identity is `(id_type, value)`
  in `instrument_identifiers`, so a rename preserves history and price continuity;
  only the **API/UI to change the symbol** is missing. (Instrument **name** edits
  are already supported — see GLOSSARY "Ticker / name change".)

All other R-items remain independent parked breadcrumbs, not part of this theme.

---


## Not on this list (recorded, but not ROADMAP)

- **SaaS/PaaS hardening layer** (D-001) — a future proprietary layer, recorded
  in an ADR rather than here. v2 core must not make choices that preclude it,
  but it is out of scope for v2 core and is not a parked ROADMAP item.
